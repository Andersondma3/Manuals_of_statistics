---
title: "Manual para el curso Modelos Lineales Avanzados (XS-0125) Segundo Examen"
author: |
  | **Autor:** Anderson Martínez <br>
  | **Colaboradores:** Sebastián Calvo, Justin Zhu, Fabián Almirall
output:
  pdf_document:
    toc: true
    toc_depth: 3
    number_sections: true
  word_document:
    toc: true
    toc_depth: '3'
---

\newpage


# Variables continuas como predictores

Este tipo de variables sería como si en el ejemplo de `Papel` que lo analizamos en el manual anterior tuviera el factor de temperatura, como una variable númerica, no como una categoría. Dado esto, para estos casos es crucial el **supuesto de linealidad**.

## Comparación del diseño de papel, cuando la variable temperatura es númerica o categórica

Para dicho experimento se tienen dos niveles:

+ Nivel 1: El número de muestras , este tiene como factor la temperatura. *Es importante notar que la temperatura puede ser un factor o una variable continua.*


+ Nivel 2: El lote, este tiene como factor el método.



*A continuación se le brinda un ejemplo de gráfico de la temperatura como categoría*


```{r, echo=FALSE, message=FALSE, fig.width=6.5, fig.height=3.5, fig.align='center', warning=FALSE}
library(ggplot2)
library(dplyr)

set.seed(123)
temperaturas <- c(200, 225, 250, 275)
lotes <- c(1, 2, 3)
n_muestras <- 4 

df_simulado_factor <- expand.grid(
  temp = temperaturas,
  lote = as.factor(lotes),
  replica = 1:n_muestras
) %>%
  mutate(
    # Convertimos explícitamente a factor para el gráfico
    temp_factor = as.factor(temp),
    
    # La simulación sigue una lógica de efectos por nivel
    res = 25 + (as.numeric(lote) * 2) + (as.numeric(temp_factor) * 2.5) + rnorm(n(), 0, 1.5)
  )

# Gráfico 1: Puntos con Temperatura como Factor en el eje X
ggplot(df_simulado_factor, aes(x = temp_factor, y = res, color = lote)) +
  geom_point(size = 2.25, alpha = 0.8, position = position_jitter(width = 0.1)) + 
  facet_wrap(~ lote) + 
  scale_color_manual(values = c("#E41A1C", "#377EB8", "#4DAF4A")) + 
  labs(
    title = "Temperatura como Variable Categórica (Factor)",
    x = "Niveles de Temperatura",
    y = "Resistencia"
  ) +
  theme_bw() +
  theme(legend.position = "none", plot.title = element_text(hjust = 0.5, face = "bold"))

```



*A continuación se le brinda un ejemplo de gráfico de la temperatura como númerica*


```{r, echo=FALSE, message=FALSE, fig.width=6.5, fig.height=3.5, fig.align='center', warning=FALSE}


# 1. Simulación de datos basada en image_664bac.png
set.seed(123)
temperaturas <- c(200, 225, 250, 275)
lotes <- c(1, 2, 3)
n_muestras <- 4 # Puntos por combinación



df_simulado <- expand.grid(
  temp = temperaturas,
  lote = as.factor(lotes),
  replica = 1:n_muestras
) %>%
  mutate(
    # Simulación de resistencia en el rango [26, 45]
    res = 25 + (as.numeric(lote) * 2) + (temp * 0.03) + rnorm(n(), 0, 1.5)
  )
ggplot(df_simulado, aes(x = temp, y = res, color = lote)) +
  geom_point(size = 2, alpha = 0.6) + # Puntos de datos observados
  
  # 2. Agregar la línea de regresión para el supuesto de linealidad
  # 'method = lm' ajusta un modelo lineal (y = a + bx)
  geom_smooth(method = "lm", se = FALSE, size = 1) + 
  
  facet_wrap(~ lote) +
  scale_color_manual(values = c("#E41A1C", "#377EB8", "#4DAF4A")) +
  labs(
    title = "Resistencia vs Temperatura",
    subtitle = "Ajuste lineal por Lote (Variable Continua)",
    x = "Temperatura ",
    y = "Resistencia"
  ) +
  theme_bw() +
  theme(
    plot.title = element_text(hjust = 0.5, face = "bold"), # Título centrado y en negrita
    plot.subtitle = element_text(hjust = 0.5),            # Subtítulo centrado
    legend.position = "none"
  )
```


Para convertir la variable temperatura a una variable númerica, es super importante que se cumpla el **suspuesto de linealidad**, para poder realizar este cambió en el análisis. Si sucede que los puntos tienen otra forma, como una cuadrática, logarítmica etc,se necesitarian más datos para asegurarlo. En cambió con la linealidad, esto no sucede así, debido a que es usual que con 4 datos está se pueda observar correctamente. Es importante resaltar que la linealidad se debe cumplir dentro de la unidad de nivel 2, en nuestro ejemplo sería, en cada lote. Si sucede la situación de que en todos los lotes, los datos están muy desordenos, es altamente recomendado trabajarlo como factor.Por último, si tenemos muchas observaciones dentro de cada lote, lo recomendable es realizar agrupaciones, para tener un mayor control de la variable.


## Modelos para variables continuas en modelos mixtos

Una vez teniendo en cuenta las diferencias entre una variable categórica y una continua, a continuación se le presentan ambos modelos:

*Los siguientes modelos, se contruyen basados en el ejemplo del experimento Papel*

### Modelo con interacción de una variable categórica



$$y_{ijkl} = \beta_0+\alpha_i+\beta_j+(\alpha\beta)_{ij}+\delta_k+\varepsilon_{ijkl}$$

donde:

+ **Los factores fijos corresponden a :** $\beta_0:$ Es el intercepto, $\alpha_i:$ Efecto del método,$\beta_j:$ Efecto de la temperatura y el efecto de interación entre estos factores $(\alpha\beta)_{ij}$


+ **Las distribuciones de los factores aleatorios, asociadas al modelo corresponden a :** $\delta_k\sim\mathcal{N}(0,\sigma^2_\delta)$. De $\varepsilon\sim\mathcal{N}(0,\sigma^2_\varepsilon)$, también esta la distribución $Y_{ij}\sim\mathcal{N}(\mu_j,\sigma^2_\varepsilon)$. Por último, sabemos que $\delta_k = \mu_k-\mu$, de esto obtenemos que $\mu_j\sim\mathcal{N}(0,\sigma^2_\delta)$

\newpage

### Modelo con interacción de una variable númerica

Para este modelo debemos pensar que la temperatura no es un factor, sino una variable númerica, que cumple con el supuesto de linealidad. Dado lo anterior, el modelo resultante queda de la siguiente manera:


$$y_{iTjkl} = \beta_0+\alpha_i+\gamma_1T+\omega_iT+\delta_{k(i)}+\lambda_{k(i)}T+\varepsilon_{ijkl}$$

*Utilizando tratamiento de referencia*

donde:

+ $\beta_0:$ Es el intercepto del modelo.

+ $\alpha_i:$ Efecto del método, con las restricciones $\alpha_1 = 0$.

+ $\gamma_1T:$ Es la pendiente base (fija) del método de referencia.

+ $\omega_iT:$ Es el ajuste fijo que se le hace a la pendiente según el método en el que esté.

+ $\delta_{k(i)}:$ Efecto del lote del i-ésimo método, el cuál tiene la distribuación $\delta_k\sim\mathcal{N}(0,\sigma^2_\delta)$.

+ $\lambda_{k(i)}:$ Es lo que aumenta o disminuye la pendiente de ese lote, con respecto a la pendiente general del método i-ésimo, lo cual es lo mismo que decir que el efecto de interacción entre lote y temperatura, el mismo se distribuye como  $\lambda_{k(1)}\sim\mathcal{N}(0,\sigma^2_\lambda)$.


**Modelo para el método 1**

$$y_{1Tjkl} = \beta_0+\gamma_1T+\delta_{k(1)}+\lambda_{k(1)}T+\varepsilon_{ijkl}$$

donde:

+ $\beta_0:$ Es el intercepto del modelo.

+ $\gamma_1:$ Es la pendiente de temperatura.

+ $\delta_{k(1)}:$ Efecto del lote dentro del método 1, este se ubica en el intercepto, el cual se distribute como $\delta_{k(1)}\sim\mathcal{N}(0,\sigma^2_\delta)$


+ Es lo que aumenta o disminuye la pendiente de ese lote, con respecto a la pendiente general del método 1  $\lambda_{k(1)}\sim\mathcal{N}(0,\sigma^2_\lambda)$

**Modelo para el método 2**

$$y_{2Tjkl} = (\beta_0+\alpha_2)+(\gamma_1+\omega_2)T+\delta_{k(2)}+\lambda_{k(2)}T+\varepsilon_{ijkl}$$

donde:

+ $\beta_0+\alpha_2:$ Es el intercepto del modelo, en este caso $\alpha_2$ representa cuando cambia la media con respecto al método 1 en el intercepto.

+ $\gamma_1+\omega_2:$ Es la pendiente.
 
 


**Modelo para el método 3**


$$y_{3Tjkl} = (\beta_0+\alpha_3)+(\gamma_1+\omega_3)T+\delta_{k(3)}+\lambda_{k(3)}T+\varepsilon_{ijkl}$$

donde:

+ $\beta_0+\alpha_3:$ Es el intercepto del modelo, en este caso $\alpha_3$ representa cuando cambia la media con respecto al método 1 en el intercepto.

+ $\gamma_1+\omega_3:$ Es la pendiente.  
 


```{r, warning=FALSE, message=FALSE, echo=FALSE, fig.width=6.5, fig.height=3.5, fig.align='center'}
# 1. Definición de parámetros
mu <- 35
beta <- 0.08
alphas <- c(-4, 0, 4) # Efec
x_start <- 200 # Inicio del eje visible

df_lineas <- data.frame( #Creación de los datos, para la simulación del gráfico
  temp = rep(seq(200, 275, length.out = 100), 3),
  metodo = rep(c("Método 1", "Método 2", "Método 3"), each = 100),
  alpha_val = rep(alphas, each = 100)
) %>%
  mutate(res = mu + alpha_val + (beta * (temp - 237.5)))

# 2. Gráfico con alpha en el intercepto (inicio del eje)
ggplot(df_lineas, aes(x = temp, y = res, color = metodo)) +
  geom_line(size = 1.1) +
  # Referencia de la Gran Media (Global Mean)
  geom_segment(aes(x = 200, xend = 275, y = mu + (beta * (200 - 237.5)), 
                   yend = mu + (beta * (275 - 237.5))), 
               color = "black", linetype = "dotted") +
  
  # --- MODIFICACIÓN DE PARÁMETROS PARA EL INTERCEPTO ---
  # Dibujamos las llaves justo en x = 200 (el intercepto del gráfico)
  annotate("segment", x = 200, xend = 200, 
           y = 32, yend = 28, # Alpha 1
           arrow = arrow(ends = "both", length = unit(0.15, "cm")), 
           color = "darkblue") +
  annotate("text", x = 203, 
           y = 30, 
           label = "alpha[2]", 
           parse = TRUE, 
           color = "darkblue") +
  
 annotate("segment", 
         x = 198, xend = 198,        # 'x' menor para que esté más atrás
         y = 36, yend = 28,          # 'y' menores para que esté abajo
         arrow = arrow(ends = "both", length = unit(0.15, "cm")), 
         color = "darkgreen") +
 annotate("text", 
         x = 195,                    # 'x' un poco antes del segmento para no traslapar
         y = 33.5,                     # 'y' central entre 32 y 28
         label = "alpha[3]", 
         parse = TRUE, 
         color = "darkgreen")+
  
  scale_color_manual(values = c("#E41A1C", "#377EB8", "#4DAF4A")) +
  labs(
    title = "Representación de los Efectos Fijos del Método",
    subtitle = expression(paste("Distancia del intercepto definida por ", alpha[i], " = ", mu[i], " - ", mu)),
    x = "Temperatura",
    y = "Resistencia"
  ) +
  theme_minimal() +
  theme(plot.title = element_text(hjust = 0.5, face = "bold"),
        plot.subtitle = element_text(hjust = 0.5))
```


Cuando no hay interacción los $\alpha$, representan la diferencias entre las medias con respecto al método 1 (referencia).En el caso cuando hay interacción, los $\alpha$ representan la diferencia cuando la temperatura es 0, lo cual no tendría ningún sentido en el experimento de papel, en otros casos ese análisis si puede tener sentido.

*A continuación se le presenta de un gráfico de cuando no se presenta interacción entre lote y método, pero si entre método y temperatura*

**Nota: Puede ser que salga una pregunta de crear este gráfico en el examen, por ello el código queará visible y comentado**


```{r, fig.width=6.5, fig.height=3.5, fig.align='center'}
# 1. Simulación de datos con Interacción 
set.seed(2026) 
temp_cont <- seq(200, 275, length.out = 50) 
metodos <- c("Metodo 1", "Metodo 2", "Metodo 3") 
df_interaccion <- expand.grid( 
  temp = temp_cont,
  metodo = metodos,
  lote_id = 1:4
) %>%
  mutate(
    lote_unico = as.factor(paste(metodo, "Lote", lote_id)),
    beta = case_when(
      metodo == "Metodo 1" ~ 0.10,   # Pendiente positiva leve
      metodo == "Metodo 2" ~ 1.76,   # Pendiente NEGATIVA muy fuerte (Cruza hacia abajo)
      metodo == "Metodo 3" ~ 3.2    # Pendiente POSITIVA muy fuerte (Sube rápido)
    ),
    # Efecto del lote (desplazamiento vertical)
    efecto_lote = lote_id * 2.5,
    # Modificamos el intercepto base para que el cruce ocurra en el centro del gráfico
    res = 200 + efecto_lote + (beta * (temp - 235)) 
  )
# 2. Cálculo de la línea de la media por método
df_medias <- df_interaccion %>%
  group_by(metodo, temp) %>%
  summarise(res_media = mean(res), .groups = 'drop')
# 3. Gráfico Único
ggplot() +
  # Líneas de los Lotes
  geom_line(data = df_interaccion, 
            aes(x = temp, y = res, color = metodo, group = lote_unico), 
            size = 0.7, alpha = 0.3) +
  
  # LÍNEAS DE LA MEDIA
  geom_line(data = df_medias, 
            aes(x = temp, y = res_media, group = metodo, color = metodo), 
            size = 1.3) +
  
  scale_color_manual(values = c("Metodo 1" = "red4", 
                                "Metodo 2" = "skyblue3", 
                                "Metodo 3" = "green4")) +
  labs(
    title = "Modelo Lineal con Interacción Fuerte Método × Temperatura",
    subtitle = "El efecto de la temperatura depende del método utilizado",
    x = "Temperatura",
    y = "Resistencia",
    color = "Método"
  ) +
  theme_minimal() +
  theme(
    plot.title = element_text(hjust = 0.5, face = "bold"),
    plot.subtitle = element_text(hjust = 0.5),
    legend.position = "bottom"
  )
```



No habría interacción entre lote y método, cuando dentro de un mismo método las lineas son paralelas, como se pudo observar en el gráfico anterior. Por lo que en este caso $\sigma_{\lambda} = 0$ 


*La representación del gráfico anterior en el modelo es la siguiente*

$$y_{iTjkl} = \beta_0+\alpha_i+\gamma_1T+\omega_iT+\delta_{k(i)}+\varepsilon_{ijkl}$$
Note que $\lambda_{k(i)}$, no se encuentra en el modelo.

*A continuación se le presenta  el caaso contrario, el cual es un gráfico de cuando se presenta interacción entre lote y método, pero no entre método y temperatura*


```{r, fig.width=6.5, fig.height=3.5, fig.align='center'}
# 1. Simulación (Mantenemos tu lógica exacta)
set.seed(2026)
temp_cont <- seq(200, 275, length.out = 50)
metodos <- c("Metodo 1", "Metodo 2", "Metodo 3")
lotes_por_metodo <- 1:4

df_pendientes <- expand.grid(
  temp = temp_cont,
  metodo = metodos,
  lote_id = lotes_por_metodo
) %>%
  group_by(metodo, lote_id) %>%
  mutate(
    lote_unico = as.factor(paste(metodo, "Lote", lote_id)),
    beta_base = 0.20,
    beta_lote = rnorm(1, mean = 0, sd = 0.15), 
    beta_final = beta_base + beta_lote,
    efecto_metodo = case_when(
      metodo == "Metodo 1" ~ 10,
      metodo == "Metodo 2" ~ 30,
      metodo == "Metodo 3" ~ 50
    ),
    res = efecto_metodo + (beta_final * (temp - 200))
  ) %>%
  ungroup()

# 2. SOLUCIÓN AL ERROR: Crear un dataframe de medias INDEPENDIENTE
# Esto garantiza que las líneas negras sean perfectamente paralelas (Pendiente = 0.20)
df_medias_p <- expand.grid(
  temp = temp_cont,
  metodo = metodos
) %>%
  mutate(
    efecto_metodo = case_when(
      metodo == "Metodo 1" ~ 10,
      metodo == "Metodo 2" ~ 30,
      metodo == "Metodo 3" ~ 50
    ),
    res_media = efecto_metodo + (0.20 * (temp - 200)) # Pendiente fija
  )

# 3. Gráfico Único
ggplot() +
  # Líneas de los lotes (Divergentes)
  geom_line(data = df_pendientes, 
            aes(x = temp, y = res, color = metodo, group = lote_unico), 
            size = 0.7, alpha = 0.4) +
  
  # Líneas de la MEDIA (Negras y PARALELAS)
  geom_line(data = df_medias_p, 
            aes(x = temp, y = res_media, group = metodo), 
            color = "black", size = 1.2) +
  
  scale_color_manual(values = c("Metodo 1" = "#E41A1C", 
                                "Metodo 2" = "#377EB8", 
                                "Metodo 3" = "#4DAF4A")) +
  labs(
    title = "Interacción Lote × Método",
    x = "Temperatura",
    y = "Resistencia",
    color = "Método"
  ) +
  theme_minimal() +
  theme(
    plot.title = element_text(hjust = 0.5, face = "bold"),
    plot.subtitle = element_text(hjust = 0.5),
    legend.position = "bottom"
  )
```




Observe que para este caso hay interacción entre lote y método, ya que las pendientes de todos los lotes son muy distintas, pero para el caso de método y temperatura no hay interacción debido a que las pendientes son las mismas. Para esta situación el $\delta_k= \mu_{k(i)}-\mu_{(i)}$, debido a que este diseño es anidado y por la interacción se tiene que considerar el método en el que se encuentra, ya que los lotes son únicos para cada método.Por último, si hay interación entonces $\sigma_{\lambda}^2 \neq 0$.


*La representación del gráfico anterior en el modelo es la siguiente*

$$y_{iTjkl} = \beta_0+\alpha_i+\gamma_1T+\delta_{k(i)}+\lambda_{k(i)}T+\varepsilon_{ijkl}$$
Note que $\omega_i$, no se encuentra en el modelo.



En muchas ocasiones nuestro objetivo no se centra en la parte aleatoria, sin embargo siempre se debe analizar primero, comenzando con la interacción aleatorio, como en este ejemplo es $\lambda_{k(i)}$.


Existe otro caso, donde no tiene sentido tener el intercepto en el modelo el cual se debe observar de la siguiente manera.

```{r, message=FALSE,fig.width=6.5, fig.height=3.5, fig.align='center', echo=FALSE}
# 1. Simulación de datos: Interacción Lote x Temperatura
set.seed(2026)
temp_cont <- seq(200, 275, length.out = 50)
metodos <- c("Metodo 1", "Metodo 2", "Metodo 3")
lotes_por_metodo <- 1:4
df_pendientes <- expand.grid(
  temp = temp_cont,
  metodo = metodos,
  lote_id = lotes_por_metodo
) %>%
  group_by(metodo, lote_id) %>%
  mutate(
    lote_unico = as.factor(paste(metodo, "Lote", lote_id)),
    # 1. NO INTERACCIÓN MÉTODO x TEMP: Pendiente base igual para todos

    beta_base = 0.20,
    # 2. INTERACCIÓN LOTE x TEMP: Variación en pendiente (beta)
    # Aumentamos el sd para que se note la diferencia de inclinación
    beta_lote = rnorm(1, mean = 0, sd = 0.15), 
    beta_final = beta_base + beta_lote,
    # Efecto del Método (Intercepto fijo por método)

    efecto_metodo = case_when(
      metodo == "Metodo 1" ~ 10,
      metodo == "Metodo 2" ~ 10,
      metodo == "Metodo 3" ~ 10
    ),
    # Resultado lineal: Todos los lotes de un método comparten el mismo intercepto
    # pero difieren en su pendiente (beta_final)
    res = efecto_metodo + (beta_final * (temp - 200))
  ) %>%
  ungroup()
# 2. Cálculo de la línea de la media por método
df_medias_p <- df_pendientes %>%
  group_by(metodo, temp) %>%
  summarise(res_media = mean(res), .groups = 'drop')
# 3. Gráfico Único
ggplot() +  # Líneas de los Lotes (Mismo origen, distinta pendiente)
  # geom_line(data = df_pendientes, 
  #           aes(x = temp, y = res, color = metodo, group = lote_unico), 
  #           size = 0.7, alpha = 0.4) +
  # Líneas de la MEDIA (
  geom_line(data = df_medias_p, 
            aes(x = temp, y = res_media, group = metodo), 
            color = "black", size = 1.2) +
  #scale_color_manual(values = c("Metodo 1" = "#E41A1C", "Metodo 2" = "#377EB8", "Metodo 3" = "#4DAF4A")) +
  labs(
    title = "Interacción Lote × Temperatura (sin intercepto)",
    x = "Temperatura",
    y = "Resistencia",
    color = "Método"
  ) +
  theme_minimal() +
  theme(
    plot.title = element_text(hjust = 0.5, face = "bold"),
    plot.subtitle = element_text(hjust = 0.5),
    legend.position = "bottom"
  )
```


*La representación del gráfico anterior en el modelo es la siguiente*

$$y_{iTjkl} = \gamma_1T+\omega_iT+\delta_{k(i)}+\lambda_{k(i)}T+\varepsilon_{ijkl}$$
Note que $\beta_0+\alpha_i$, no se encuentra en el modelo esto debido a que no hay intercepto.


```{r, echo=FALSE,fig.width=6.5, fig.height=3.5, fig.align='center'}

# 1. Simulación de un solo método con interacción en la pendiente
set.seed(2026)
temp_cont <- seq(200, 275, length.out = 50)
n_lotes <- 3

df_delta <- expand.grid(
  temp = temp_cont,
  lote_id = 1:n_lotes
) %>%
  mutate(
    lote_unico = as.factor(paste("Lote", lote_id)),
    beta_base = 0.20, # Pendiente media del método
    # El Lote 2 tendrá una interacción marcada (delta positivo)
    beta_lote = case_when(
      lote_id == 1 ~ -0.08,
      lote_id == 2 ~ 0.15,  # Este es nuestro delta notable
      lote_id == 3 ~ -0.05
    ),
    beta_final = beta_base + beta_lote,
    res = 30 + (beta_final * (temp - 200))
  )

# 2. Línea de la media teórica
df_media_unica <- data.frame(temp = temp_cont) %>%
  mutate(res_media = 30 + (0.20 * (temp - 200)))

# 3. Gráfico con la anotación de Delta
ggplot() +
  # Líneas de los lotes
  geom_line(data = df_delta, 
            aes(x = temp, y = res, color = lote_unico, group = lote_unico), 
            size = 0.8, alpha = 0.5) +
  # Línea de la MEDIA (Referencia)
  geom_line(data = df_media_unica, 
            aes(x = temp, y = res_media), 
            color ="red2", size = 1.5) +
  # ANOTACIÓN DE DELTA (Distancia al final del eje)
  annotate("segment", x = 270, xend = 270, 
           y = 44, yend = 54.5, # Distancia entre media y Lote 2 al final
           arrow = arrow(ends = "both", length = unit(0.2, "cm")), 
           color = "black", size = 1) +
  annotate("text", x = 273, y = 49, 
           label = "delta[k]", parse = TRUE, 
           color = "black", size = 5) +
  scale_color_manual(values = c("Lote 1" = "blue2", "Lote 2" = "blue2", "Lote 3" = "blue2")) +
  labs(
    title = "Efecto de Interacción Lote × Temperatura",
    subtitle = "Delta representa la distancia de la pendiente del lote respecto a la media",
    x = "Temperatura", y = "Resistencia", color = "Lote"
  ) +
  theme_minimal() +
  theme(plot.title = element_text(hjust = 0.5, face = "bold"),
        plot.subtitle = element_text(hjust = 0.5))
```


# Análisis del experimento papel

```{r, echo=FALSE}
load("papel.Rdata")
base$metodo=factor(base$metodo)
base$lote=factor(base$lote)
```



```{r,fig.width=6.5, fig.height=3.5, fig.align='center'}
library(lattice)
xyplot(res~temp|lote,type=c("r","p"),xlab="temperatura",
ylab="resistencia",data=base)
```

Observe que en este caso, en la mayoría hay linealidad, en algunos hay otros comportamientos, pero los mismos no son consistentes. Además con solo cuatro puntos no podemos asegurar ningún otro tipo de comportamiento, por lo que asumimos que se cumple el supuesto de linealidad.


*De la siguiente forma, obtenemos cuantos lotes por cada método tenemos *

```{r}
apply(table(base$metodo, base$lote) > 0,1,sum)
```

*A continuación se gráfican los lotes dentro de cada método*

```{r,fig.width=6.0, fig.height=3.0, fig.align='center'}
xyplot(res~temp|metodo,groups=lote,type=c("r"),xlab="temperatura",
ylab="resistencia",layout=c(3,1),data=base)
```

En este caso note que las pendientes de los lotes no son iguales dentro de cada método, por lo que se podría sospechar de una posible interacción entre lote y temperatura.

*A continuación se gráfican las lineas de cada método*

```{r,fig.width=6.5, fig.height=3.5, fig.align='center'}
xyplot(res~temp,groups=metodo,type=c("r"),xlab="temperatura",
ylab="resistencia",data=base,auto.key = T)
```

Apesar de que se observe una pequeña interacción, no es tan fuerte como la que se vió en el gráfico anterior. *Es importante resaltar que esto lo estamos haciendo debido a que la temperatura es bastante lineal*.

**Nota:** La ventaja de que la temperatura sea númerica y super lineal, es que se puede usar para interpolar valores para cualquier temperatura.

**Las ecuaciones de las rectas para cada método son las siguientes**

**Método 1**

$\mu_{1,T}= \beta_0+\beta_1T$


**Método 2**

$\mu_{2,T}= (\beta_0+\alpha_2)+(\beta_1+\gamma_2)T$


**Método 3**

$\mu_{3,T}= (\beta_0+\alpha_3)+(\beta_1+\gamma_3)T$


Note que mediante las restas de estas ecuaciones cuando $T = 0$, obtenemos los $\alpha_i$


## Modelo completo para el experimento de papel

Se tiene el siguiente modelo:


$$y_{iTjkl} = \beta_0+\beta_1T+\alpha_i+\gamma_jT+\delta_{k(i)}+\lambda_{k(i)}T+\varepsilon_{ijkl}$$
donde:

Usando tratamiento de referencia

+ $\alpha_i:$ Efecto del i-ésimo método en el intercepto,con las retricciones $\alpha_1 = 0$ y que $\alpha_2 = \mu_{2,T=0}-\mu_{1,T=0}$, *La representación gráfica de estos efectos se encuentran en la sección de arriba*.

+ $\beta_1:$ Pendiente de la temperatura.

+ $\gamma_j;$ Efecto j-ésimo de interacción entre el método y temperatura, con la restricción $\gamma_1 = 0$, este significa cuánto aumenta la temperatura con respecto al método 1. 


+ $\delta_k:$ Efecto del lote con respecto a la linea media del método al que corresponde en el intercepto, el cuál se distribuye como $\delta_k~\sim\mathcal{N}(0,\sigma_\delta^2)$. *Recuerde que en la sección anterior hay una representación gráfica de este efecto*.

+ $\lambda_{k(i)}:$ Es el efecto de interacción entre lote y temperatura, el cual significa cuánto cambia la pendiente del k-ésimo lote con respecto a la pendiente general del i-ésimo método.Este se distribuye como  $\lambda_{k(i)}\sim\mathcal{N}(0,\sigma_\lambda)$


### Sobre la interacción entre los factores aleatorios 

Tenemos los siguientes casos

+ Si $\rho = 0$, entonces $\delta_k$ y $\lambda_{k(i)}$ se distribuyen de forma independiente.


+ $\rho \neq 0$, entonces $(\delta_k, \lambda_{k(i)}) \sim \mathcal{N}_2 \left( \begin{pmatrix} 0 \\ 0 \end{pmatrix}, \boldsymbol{\Sigma} \right)$.

*La matriz de covarianzas se contruye de la siguiente manera*

$$\boldsymbol{\Sigma} = \begin{pmatrix} 
\sigma_\delta^2 & \sigma_{\delta\lambda} \\ 
\sigma_{\lambda\delta} & \sigma_\lambda^2 
\end{pmatrix}$$


Además de lo anterior tenemos lo siguiente:

$$\rho = \frac{\sigma_{\delta\lambda}}{\sigma_\delta\sigma_\lambda} \implies\sigma_{\delta\lambda} = \rho\space\sigma_\delta\space\sigma_\lambda $$


*A continuación se gráfica, como se ve* $\rho$ *según cada caso*

```{r, echo=FALSE,fig.width=6.5, fig.height=3.5, fig.align='center', warning=FALSE}
library(mvtnorm)

# Configuración de la matriz de gráficos (1 fila, 3 columnas)
par(mfrow = c(1, 3), mar = c(1, 1, 3, 1))

# Secuencia para los ejes X e Y
x <- seq(-3, 3, length = 50)
y <- seq(-3, 3, length = 50)
mu <- c(0, 0)

# Función para graficar la superficie según rho
graficar_persp <- function(rho, titulo) {
  # Definir matriz de varianza-covarianza
  sigma <- matrix(c(1, rho, rho, 1), ncol = 2)
  
  # Calcular la densidad bivariada
  z <- outer(x, y, function(x, y) dmvnorm(cbind(x, y), mean = mu, sigma = sigma))
  
  # Generar el gráfico 3D
  persp(x, y, z, 
        theta = 30, phi = 30, # Ángulos de visión
        expand = 0.5, 
        col = "lightblue", 
        shade = 0.5,
        ticktype = "detailed",
        main = titulo,
        zlab = "f(x,y)",
        cex.main = 1.5)
}

# 1. Independencia: rho = 0
graficar_persp(0, expression(rho == 0))

# 2. Correlación Media: rho = 0.5
graficar_persp(0.5, expression(rho == 0.5))

# 3. Dependencia Fuerte: rho = 0.99
# Nota: Usamos 0.99 porque 1 hace que la matriz sea singular y persp falle
graficar_persp(0.99, expression(rho == 0.99))

# Restaurar configuración original de par
par(mfrow = c(1, 1))
```



### Resumen de pruebas de hipótesis a realizar 

1. $H_0: \rho = 0$

2. $H_0: \sigma_\lambda = 0$. En ocasiones si no hay correlación este paso puede omitirse.

3. $H_0: \sigma_{\delta}:$ Hay que tener cuidado, porque **depende del sueño este no se puede eliminar**

4. $H_0: \gamma_j = 0$

+ Rechazo: Hay interacción entre método y temperatura, *Ver el gráfico de la sección de arriba*. 

+ No rechazo: No hay interacción entre método y temperatura ,*Ver el gráfico de la sección de arriba*. Luego de estos realizamos las hipótesis de los demás factores fijos $(H_0:\alpha_i = 0)$. Si son significativos realizamos los intervalos de confianza, si sucede el caso contrario se debe anailizar potencia.


5. $H_0: \beta_1 = 0$



*Nota: Cuando la diferencia es significativa, pero no relevante, esto se debe a la poca variabilidad y cuando una muestra es muy grande*

# Correlación  de los interceptos

Tenemos la siguiente ecuación del lote k-ésimo:

$$\mu_{k} = (\beta_0+\alpha_j+\delta_k)+(\beta_1+\gamma_j+\lambda_k)T$$

Lo que se quiere observar si $\delta_k$ y $\lambda_k$, están correlacionados, para ello tenemos que calcular las diferentes rectas para cada lote s

```{r,fig.width=6.5, fig.height=3.5, fig.align='center'}
beta0=beta1=c()
for(i in 1:9) { # Ojo que son 9 lotes
mod=lm(res~temp,base[base$lote==i,]) #Los lotes está enumerado del 1 al 9
beta0[i]=mod$coef[1]
beta1[i]=mod$coef[2]
}
plot(beta1,beta0,pch=18,col=4)
abline(lm(beta0~beta1))
```

Observe que conforme el intercepto baja, la pendiente aumenta, por lo que se puede sospechar una correlación negativa fuertes entre $\beta_0$ y $\beta_1$. Lo mismo lo podemos verificar con el siguiente código:

```{r}
cor(beta0,beta1)
```

Observe que la correlación es -0.95, esto nos confirma lo visto en el gráfico anterior. En términos del experimento de `papel`, esto nos dice que un lote que comienza con baja resistencia al principio tiene un crecimiento más rápido en la resistencia, comparado con un lote que comienza con alta resistencia, esto tiene sentido debido que si se tiene baja resistencia al principio el margen de mejora va a ser mayor, en comparación que cuando se empieza con una buena resistencia.

## Mover la variable para descubirir el intercepto

Para obtener verdaderamente de donde comienza el intercepto, lo que hacemos es mover la variable de modo que el intercepto verdaderamente represente el inicio de la variable, como por ejemplo en el experimento de papel debido a que la temperatura comienza en 200, restamos esos 200 para que comience en cero. Es importante aclarar que esto nos funciona, para variables que tiene un mínimo.**Esto va a afectar la correlación entre interceptos, por lo que se recomienda hacer cuando tenga sentido.**

*Ejemplo del proceso de mover la variable*

$$
\begin{aligned}
temp1 &= temp - 200 \\
\text{Si } temp = 200 &\implies temp1 = 0 \\
\text{Si } temp = 225 &\implies temp1 = 25 \\
\text{Si } temp = 250 &\implies temp1 = 50 \\
\text{Si } temp = 275 &\implies temp1 = 75
\end{aligned}
$$

## Análisis del experimento
$$H_0: \rho = 0 $$

**Modelo correlacionado, ya que los coeficientes ($\beta_0$ y $\beta_1$) están juntos dentro del lote.**

```{r, message=FALSE}
library(lme4)
mod1=lmer(res~1+metodo*temp+(1+temp|lote),data=base)
```


**Modelo no correlacionado, se le pone el, 0 para que ignore el intercepto.**

```{r, warning=FALSE}
mod2=lmer(res~1+metodo*temp+(1|lote)+(0+temp|lote),data=base)
# Se puede colocar 0 o -1, par ignorar el intercepto
```


**Comparación del modelo**


```{r}
anova(mod1,mod2,test="LRT")
```

Observe que no se rechaza la hipótesis, por lo que asumimos que no hay correlación.

\newpage

**Luego lo que queremos es ver la interacción, por lo que se prueba la siguiente hipótesis**




$$H_0:\sigma_{\lambda} = 0$$

```{r, warning=FALSE}
mod2=lmer(res~1+metodo*temp+(1|lote)+(0+temp|lote),data=base)
mod3=lmer(res~metodo*temp+(1|lote),base)
anova(mod2,mod3)
```


No se rechaza la hipótesis, por lo que no son diferentes las pendientes entre los lotes de un mismo método. Cuando hay muy pocas unidades en el nivel 2 es muy díficil encontrar diferencias entre las pendientes.


**Ahora queremos ver si hay interacción, pero en la parte fija**

```{r}
drop1(mod3,test="Chisq")
```

No resulta significativa la interacción entre temperatura y método, por lo que asumimos que no hay, con esto podemos decir que podemos comparar los métodos sin importar la temperatura.


**Luego se pone a prueba la significancia de la temperatura**

$$H_0:\beta_1 = 0$$

```{r}
mod4=lmer(res~metodo+temp+(1|lote),base)
drop1(mod4,test="Chisq")
```


Debido a que p < 0.001, podemos decir que el coefiente de temperatura es diferente de 0.





```{r}
fixef(mod4)
```

**El coeficiente de temperatura se interpreta para un aumento de temperatura que tenga sentido, por ejemplo, 25 grados. Entonces por cada 25 grados que aumente la temperatura se espera que la resistencia promedio aumente (0.1209x25=3.02) 3.02 kg/cm, en cualquiera de los 3 métodos.**


```{r}
confint(mod4)[6,]*25
```


**El aumento en la resistencia es entre 2.34 y 3.70 kg/cm por cada 25 grados que aumente la temperatura, en cualquiera de los 3 métodos**


```{r, warning=FALSE, message=FALSE}
library(emmeans)
emmeans(mod4,pairwise~metodo,adjust="bonferroni")$contrast
```

*Nota: Si el intervalo es significativo, pero no es relevante, esto sucede cuando la muestra es muy grande.*

\newpage

# Análisis del estudio de las playas


```{r}
load("riqueza.Rdata")
base$riq = apply(base[, 2:76] > 0,1,sum)# Se suma por fila, para contar el número de especies
```

## Gráfico para ver linealidad

```{r,message=FALSE, fig.width=6.5, fig.height=3.5, fig.align='center'}
xyplot(riq~NAP|playa,pch=18,
ylab="riqueza promedio",type=c("p","r"),data=base)
```

En la mayoría de las playas, los puntos siguen una tendencia bastante lineal, por lo que parece acertado seguir con una regresión lineal.


**El modelo para este experimento es el siguiente:**


$$y_{NAP,k,l} = \beta_0+\beta_1NAP+\delta_k+\gamma_{k}NAP+\varepsilon_{NAP,k,l}$$

$\gamma_{k}:$ Este nos indica que cada playa tendrá una pendiente diferente

$\delta_k:$ Cuánto sube la riqueza promedio, respecto a la media general cuando $NAP = 0$, en la playa k-ésima.

$\gamma_{k}:$ Cuánto cambia la pendiente de NAP de la playa k-ésima con respecto a la pendiente general.

$y_{NAP,k,l}:$ Representa la observación de un NAP particular, en una playa, de una cierta observación.

*Nota: En los efectos aleatorios, le media cero, representa el modelo de suma nula*.


$\text{Si }   \rho \neq 0 \implies (\delta_k, \gamma_k) \sim \mathcal{N}_2 \left( \begin{pmatrix} 0 \\ 0 \end{pmatrix}, \boldsymbol{\Sigma} \right), \text{por lo que no tiene sentido eliminar } \gamma_k$


$\text{Si }   \rho = 0 \implies \delta_k \sim \mathcal{N} (0,\sigma_\delta)$ y $\gamma_k \sim \mathcal{N} (0,\sigma_\gamma)$

\newpage

*En r esto lo probamos de la siguiente manera*

```{r, warning=FALSE}
# mod1 es con \rho diferente de 0 
mod1=lmer(riq~1+NAP+(1+NAP|playa),data=base) # 1+NAP|playa, pendiente e intercepto de cada playa
# mod2 es con \rho = 0
mod2=lmer(riq~1+NAP+(1|playa)+(0+NAP|playa),data=base)
```

```{r}
anova(mod1,mod2,test="LRT")
```
Observe que si existe correlación entre el `NAP` y la `playa`


**Ahora lo que queremos probar es la signficancia del `NAP`, por lo que probamos la siguiente hipótesis:**

$$H_0: \beta_1 = 0 $$

```{r}
drop1(mod1, test = "Chisq")#Recuerde que beta1 es fijo   
```


Debido a que p = 0.001 < 0.05, esto nos dice que el `NAP`, tiene un efecto lineal en la respuesta promedio, como las lineas son decrecientes, entonces podemos afirmar que conforme sube el `NAP`, baja la riqueza promedio. Lo anterior lo sabemos por el signo del coeficiente.

```{r}
fixef(mod1)[2]
```
Esto nos indica que conforma el `NAP` aumenta una unidad, la riqueza promedio baja 2.83 unidades

**Dado el resultado anterior, obtenemos su intervalo de confianza**

```{r, warning=FALSE}
confint(mod1)
```


En este caso `.sig02`, corresponde a $\sigma_\gamma$, note que llega hasta infinito, esto se debe a la poca cantidad de datos y a una playa que contiene un valor de influencia que puede estar generando problemas. Una solución ante esto puede ser, recolectar más muestra o también recolectar más datos con diferentes tipos de NAP, en cada playa.

En la parte aleatoria, `.sig01` es la desviación estándar de los interceptos aleatorios, `.sig03` es la correlación entre interceptos y pendientes `.sigma` es la desviación estándar del error. Es evidente que el intervalo para la correlación no incluye al cero pues va de -1 a -0.36, lo que indica que sí hay una correlación importante entre los interceptos y las pendientes.

El intercepto representa la riqueza promedio cuando el NAP es cero, es decir cuando se ubica en el nivel medio de la marea, entonces, entre más alta es la riqueza promedio al nivel medio de la marea, se va a tener un mayor decrecimiento en la riqueza al aumentar el NAP, lo cual tiene sentido.

Se puede dar una interpretación al coeficiente fijo de NAP que corresponde a la pendiente general fija. Aquí se puede decir que como medida general de la tendencia (sin tomar en cuenta una playa particular), al aumentar el NAP en una unidad, la cantidad de especies disminuye en promedio entre 1.3 y 4.4 especies, con 95% de confianza. Además, el intercepto general está entre 3.97 y 9.18 lo cual representa el rango en que se espera que se encuentre el número de especies promedio cuando se ubica en el nivel medio de la marea.


## ¿Cómo encontrar una pendiente relevante?

Encontrar un valor relevante a partir de los datos es engañoso, para ello se necesitará la ayuda del criterio de un experto. Por ejemplo si el experto nos indica que dos poblaciones deben tener una diferencia de 2 unidades y en el estudio que se realiza una población tiene una riqueza promedio de 8 y otra con una población con una riqueza promedio de 10. Entonces eso quiere decir que al aumentar una distancia una desviación estándar de `NAP`, la riqueza debe aumentar dos unidades en promedio, esto nos lleva a que $\beta_1 = \frac{2}{\sigma}$. 


## Código de gráfico de interceptos y pendientes para identificadores únicos 

Este código es un ejemplo de un estudio de medidas repetidas

```{r, eval=FALSE,fig.width=6.5, fig.height=3.5, fig.align='center'}
beta0=beta1=c()
ind=as.numeric(names(table(base$sujeto)))
for(i in 1:18) {
mod=lm(reac~dias,base[base$sujeto==ind[i],])
beta0[i]=mod$coef[1]
beta1[i]=mod$coef[2]
}
plot(beta1,beta0,pch=18)
abline(lm(beta0~beta1))
```
# Análisis del experimento de ortodoncia 

A continuación se le brinda el contexto del experimento:

*Se realizó un estudio para dar seguimiento al crecimiento óseo de la maxila o mandíbula de 27 niños (16 hombres y 11 mujeres) desde los 8 a los 14 años.  Cada dos años se midió la distancia entre la pituitaria y la escotadura pterygomaxilar, dos puntos que son fácilmente identificados con rayos X. Esta distancia se utiliza para conocer el grado de maduración esquelética del individuo y está medida en milímetros.  Se compara el ritmo de crecimiento de esta distancia entre hombres y mujeres.*
   
   
Antes de iniciar el análisis completo, se tiene que mover la variable `edad`, ya que el punto de inicio de este estudio es cuando la persona tiene 8 años, por lo que le restamos 8 a la variable original, para de esa manera obtener el inicio en 0 de la variable `edad` . Esto lo hacemos porque no tiene lógica realizar el modelo para un niño de 0 años. También que ninguno de nuestros datos tiene ese valor, por lo que no es correcto extrapolar en dicha situación.   
   
```{r}
load("ortodoncia.Rdata")
base$edad1=base$edad-8
```


1. Análisis del supuesto de linealidad

Recuerde que primero análizamos la linealidad. Además lo que nos interesa es ver como nuestra variable respuesta va en crecimiento, debido al contexto en el que se encuentra este experimento, ya que no tiene sentido que una persona se le disminuya la mandíbual conforme va aumentando su edad

```{r,fig.width=6.5, fig.height=3.5, fig.align='center'}
library(lattice)
xyplot(distancia~edad1|sujeto,pch=18,
xlab="tiempo a partir de 8 años",
ylab="distancia promedio",type=c("p","r"),data=base,
layout=c(9,3))
```

Se puede observar en el gráfico que no hay problemas con la linealidad de nuestra variable, talvez en alguno niños se puede ver mucha variabilidad y datos extraños, pero eso no nos afecta para poder decir que se cumple este supuesto.

2. Luego de ver la linealidad, pasamos a analizar los interceptos para cada sujeto 

```{r}
beta0=beta1=c()
length(table(base$sujeto))
```


```{r,message=FALSE, fig.width=6.5, fig.height=3.5, fig.align='center'}
suj=as.numeric(base$sujeto)
for(i in 1:27) {
mod=lm(distancia~edad1,base[suj==i,])
beta0[i]=mod$coef[1]
beta1[i]=mod$coef[2]
}
plot(beta0,beta1,pch=18, col=4)
```


```{r}
cor(beta0,beta1)
```
En general se ve que las pendientes no van decreciendo conforme aumenta el intercepto. La correlación es -0.2 la cual no es muy alta. No parece haber una relación fuerte entre la distancia a los 8 años y la pendiente de cada niño o niña.

3. Análisis de interacción entre distintos factores


Para el siguiente gráfico, se observan todas la pendientes de cada niño o niña, pero se separan por cada sexo

```{r,fig.width=6.5, fig.height=3.5, fig.align='center'}
xyplot(distancia~edad1|sexo,group=sujeto,pch=18,data=base,
xlab="tiempo a partir de 8 años",
ylab="distancia promedio",type=c("r"))
```

Es un poco complicado analizar con este gráfico debido a la gran cantidad de lineas , lo que se hace en este tipo de situaciones es realizar los gráficos con las lineas promedio para cada uno de los sexos, tal y como se observa a continuación:

```{r,fig.width=6.5, fig.height=3.5, fig.align='center'}
xyplot(distancia~edad1,group=sexo,col=c(2,4),
xlab="tiempo a partir de 8 años",
ylab="distancia promedio",type="r",data=base,
key=list(corner=c(0,1),lines=list(col=c(4,2),lty=1),
text=list(c("masculino","femenino"))))
```

En los gráficos se puede ver que las líneas de crecimiento solo para hombres o solo para mujeres muestran pendientes muy parecidas dentro de cada grupo. Además, haciendo solo la línea de tendencia para hombres y mujeres (segundo gráfico) se nota un crecimiento más alto para los hombres. Podría ser que la interacción fija entre edad y sexo sea significativa. De todas formas se puede apreciar cómo independientemente de la edad, la distancia promedio para los hombres es siempre mayor que la de las mujeres.

 
4. Análisis con el modelo 

Una vez realizado el análisis gráfico de la interacción, tenemos el siguiente modelo:



$$Y_{ikl,E} = \beta_0+\beta_1E+\alpha_i+\gamma_iE+\delta_k+\tau_kE +\varepsilon_{ikl}$$
*Usando el modelo de suma nula*

donde;:

+ $\beta_0:$ Intercepto del modelo

+ $\beta_1:$ Pendiente de edad

+ $\alpha_i:$ Efecto i-ésimo sexo, con la restricción $\alpha_1 = -\alpha_2$

+ $\gamma_i:$ Efecto i-ésimo de interacción entre la edad y el sexo, donde $\gamma_1 = -\gamma_2$

+ $\delta_k:$ Efecto aleatorio del k-ésimo niño (sujeto), el cual se distribuye como $\mathcal{N}(0,\sigma_\delta)$

+ $\tau_k$ Efecto de k-ésimo de la interacción entre la edad y el niño (sujeto), el cual se distribuye como $\mathcal{N}(0,\sigma_\tau)$ DUDA



Recuerde que lo primero que se elimina del modelo es la interacción entre factores aleatorios, dicha prueba de hipótesis corresponde a $H_0: \rho = 0$, para realizar se debe hacer lo siguiente:

```{r, message=FALSE}
library(lme4)
options(contrasts=c("contr.sum","contr.poly")) #Suma nula 
mod1=lmer(distancia~edad1*sexo+(1+edad1|sujeto),data=base) #Modelo con correlación
mod2=lmer(distancia~edad1*sexo+(1|sujeto)+(0+edad1|sujeto),data=base) #Modelo sin correlación
anova(mod1,mod2)
```


Como la probabilidad asociada al comparar los dos modelos es alta (0.96), no se rechaza la hipótesis que dice que no hay correlación entre pendientes e interceptos. Se escoge el mod2 para continuar con el análisis. 

Después se hace un modelo que no tenga las pendientes aleatorias (`mod3`) y se compara con el modelo que sí tenía esas pendientes (`mod2`)

```{r}
mod3=lmer(distancia~edad1*sexo+(1|sujeto),data=base)
anova(mod3,mod2)
```


Nuevamente la probabilidad asociada a la prueba es alta (0.36), esto nos lleva a escoger el modelo más simple que descarta pendientes específicas para cada sujeto. **Esto no indica que las pendientes son iguales para todos los sujetos de un mismo sexo**.


Ahora se verifica si la interacción entre edad y sexo es significativa. Para esto se hace un modelo similar al mod3 pero donde se ha quitado la interacción entre edad y sexo (`mod4`). Como son factores fijos realizamos un `drop1`

```{r}
drop1(mod3,test="Chisq")
```

Al obtener una probabilidad tan baja (0.013), se rechaza la hipótesis de no interacción. Como la interacción sí es significativa, se concluye que la tasa de crecimiento de la distancia no es la misma en niños que en niñas. *Se concluye que la tasa de crecimiento de la distancia no es la misma en niños que en niñas.* Debido a que las lineas promedio para cada sexo, son distintas, tal y como se vió en el gráfico que se hizo anteriormente.

5. Construcción del modelo para cada sexo en específico. Para realizar ello primero debemos ver como está códificado, para sexo en nuestro modelo 

```{r}
contrasts(base$sexo)
```

**Hombres**

$\mu_{H,E} = (\beta_0-\alpha_1)+(\beta_1-\gamma_1)E$

**Mujeres**


$\mu_{H,E} = (\beta_0+\alpha_2)+(\beta_1+\gamma_2)E$


6. Estimaciones la ecuación de la linea para cada sexo

Primero debemos obtener la estimación de los efectos fijos del modelo 

```{r}
round(fixef(mod3),3)
```

**Hombres**

$\hat{y}_H = (21.912+0.703)+(0.632+0.152)E = 22.615+0.784E$


**Mujeres**

$\hat{y}_M = (21.912-0.703)+(0.632-0.152)E = 21.209+0.480E$


**Interpretación:** De forma puntual, se puede decir que en los hombres, por cada año, la distancia aumenta en promedio 0.784mm, mientras que en las mujeres, esta distancia aumenta en promedio 0.48mm por año. Se puede ver que la tasa de aumento para los niños es mayor.


**Opción con el modelo de tratamiento de referencia**

El modelo general en este caso es:

$$\mu_{E} = \beta_0+\beta_1E+\alpha_2+\gamma_2E$$

**Recuerde** que este modelo tiene las restricciones $\alpha_1 = 0$ y $\gamma_1 = 0$

```{r}
options(contrasts=c("contr.treatment","contr.poly"))
```

Observe la siguiente codificación

```{r}
contrasts(base$sexo)
```
```{r}
mod3=lmer(distancia~edad1*sexo+(1|sujeto),data=base)
round(fixef(mod3),3)
```

Dados estos coeficientes tenemos las nuevas ecuaciones para cada sexo:

**Hombres**

$\hat{y}_H = (21.209+1.407)+(0.480+0.305)E = 22.615+0.784E$


**Mujeres**

$\hat{y}_M = 21.209+0.480E$


Observe que dan iguales a los que obtuvimos con el modelo de suma nula, por lo que cualquier modelo que se use nos lleva a las mismas ecuaciones


## Estimación puntual para los 14 años en cada sexo

Realizandolo desde los coeficientes del modelo

```{r}
22.615-21.209
```

```{r}
22.615-21.209+(0.784-0.480)*6
# Se coloca un 6, debido a que movimos la variable (edad-8)
```


**Cálculo por medio de los vectores**

Tenemos estas dos ecuaciones de los modelos:

**Hombres**

$\mu_{H,E} = (\beta_0-\alpha_1)+(\beta_1-\gamma_1)E$ , recuerde que $\alpha_2 = -\alpha_1$

**Mujeres**


$\mu_{M,E} = (\beta_0+\alpha_1)+(\beta_1+\gamma_1)E$

Si restamos estas dos ecuaciones y llegamos a este resultado:

$$-2\alpha_1-2\gamma_1E$$
Con lo anterior obtenemos el siguiente vector:

$$(0,\, 0,\, -2,\, 0,-2E)$$

```{r,echo=FALSE}
options(contrasts=c("contr.sum","contr.poly"))
mod3=lmer(distancia~edad1*sexo+(1|sujeto),data=base)
```

Recordemos que para este caso queremos ver esa diferencia eentre hombres y mujeres es cuando el valor de `edad1 = 6` (14 años).

```{r}
c(0,0,-2,-12)%*%fixef(mod3)
#Note que da el mismo resultado que el proceso realizado anteriormente 
```

La distancia a los 8 años está dada por los interceptos de las ecuaciones escritas anteriormente. A los 8 años la distancia promedio para los hombres es 22.615mm y para las mujeres 21.209mm, entonces el promedio de esta distancia es 1.406mm mayor para los hombres que para las mujeres. A los 14 años esta diferencia aumenta considerablemente, ya que ahora la distancia promedio para los hombres es 3.23mm mayor que para las mujeres.

7. Cálculo de intervalo de confianza

```{r}
h = c(0,0,-2,-12)
df = 25
L = t(h)%*%fixef(mod3) #en este casi no hay transpuesta porque tienen la misma dimensión
ee = t(h)%*%vcov(mod3)%*%h
t = qt(1-0.05/2,25)
ic = cbind(L-t*ee,L+t*ee)
ic
```


Una conclusión que podemos dar de este estudio es la siguiente: *desde que los niños entran al estudio a los 8 años se tiene una diferencia importante en la distancia media estudiada entre hombres y mujeres; sin embargo, con el tiempo, al ir creciendo esta distancia también se acentúan las diferencias.*

# Prueba para observar donde hay diferencia entre pendientes

```{r, echo=FALSE}
options(contrasts=c("contr.treatment","contr.poly"))
```


```{r,message=FALSE}
load("dientes.Rdata")
mod1=lmer(libcaacum~cem*dia+(1+dia|diente1),base) 
drop1(mod1,test="Chisq") 
```
Note que si hay direncias entre pendientes. Una vez teniendo esto en cuenta construimos los vectores apartir del siguiente modelo


```{r}
b=fixef(mod1)
b
```
*A continuación se construyen los vectores*

```{r}
p1=c(0,0,0,1,0,0) 
p2=c(0,0,0,1,1,0) 
p3=c(0,0,0,1,0,1) 
p=cbind(p1,p2,p3) 
t(p)%*%b
```
```{r}
v12=p1-p2 
v13=p1-p3 
v23=p2-p3 
 
h=cbind(v12,v13,v23) 
L=t(h)%*%b 
L 
```

```{r}
ee=sqrt(diag(t(h)%*%vcov(mod1)%*%h)) 
q=L/ee 
prob=pt(q,21,lower.tail=F) 
# Para obtener los grados de libertad haga un emmeans y los obtiene de ahí 
round(prob,2) 
```
```{r}
prob<0.05/3 # Prueba de una cola
```

# Comparación de promedios en un momento específico de la covariable

*Esto se basa en el modelo presentado anteriormente*

```{r}
d=15 #dias
m1=c(1,0,0,d,0,0) 
m2=c(1,1,0,d,d,0) 
m3=c(1,0,1,d,0,d) 
m12=m1-m2 
m13=m1-m3 
m23=m2-m3 
h=cbind(m12,m13,m23) 
L=t(h)%*%b 
L 
```

```{r}
ee=sqrt(diag(t(h)%*%vcov(mod1)%*%h)) 
q=abs(L)/ee
prob=pt(q,21,lower.tail=F) 
round(prob,2)
```
```{r}
prob<0.05/3 
```

```{r}
t=qt(1-0.05/2,21) # los gl se obtienen de un emmmeas
lim=L[-1]-t*ee[-1] 
 
round(lim,2)
```

\newpage

# Alfabeto Griego en LaTeX

A continuación se presenta una guía rápida con las letras del alfabeto griego, su visualización compilada y el código correspondiente tanto para minúsculas como para mayúsculas.

| Nombre | Minúscula | Código Minúscula | Mayúscula | Código Mayúscula |
| :--- | :---: | :--- | :---: | :--- |
| Alpha | $\alpha$ | `$\alpha$` | $\mathrm{A}$ | `$\mathrm{A}$` o `A` |
| Beta | $\beta$ | `$\beta$` | $\mathrm{B}$ | `$\mathrm{B}$` o `B` |
| Gamma | $\gamma$ | `$\gamma$` | $\Gamma$ | `$\Gamma$` |
| Delta | $\delta$ | `$\delta$` | $\Delta$ | `$\Delta$` |
| Epsilon | $\epsilon$ o $\varepsilon$ | `$\epsilon$` o `$\varepsilon$` | $\mathrm{E}$ | `$\mathrm{E}$` o `E` |
| Zeta | $\zeta$ | `$\zeta$` | $\mathrm{Z}$ | `$\mathrm{Z}$` o `Z` |
| Eta | $\eta$ | `$\eta$` | $\mathrm{H}$ | `$\mathrm{H}$` o `H` |
| Theta | $\theta$ o $\vartheta$ | `$\theta$` o `$\vartheta$` | $\Theta$ | `$\Theta$` |
| Iota | $\iota$ | `$\iota$` | $\mathrm{I}$ | `$\mathrm{I}$` o `I` |
| Kappa | $\kappa$ | `$\kappa$` | $\mathrm{K}$ | `$\mathrm{K}$` o `K` |
| Lambda | $\lambda$ | `$\lambda$` | $\Lambda$ | `$\Lambda$` |
| Mu (Miu) | $\mu$ | `$\mu$` | $\mathrm{M}$ | `$\mathrm{M}$` o `M` |
| Nu (Niu) | $\nu$ | `$\nu$` | $\mathrm{N}$ | `$\mathrm{N}$` o `N` |
| Xi | $\xi$ | `$\xi$` | $\Xi$ | `$\Xi$` |
| Omicron | $o$ | `$o$` | $\mathrm{O}$ | `$\mathrm{O}$` o `O` |
| Pi | $\pi$ | `$\pi$` | $\Pi$ | `$\Pi$` |
| Rho | $\rho$ o $\varrho$ | `$\rho$` o `$\varrho$` | $\mathrm{P}$ | `$\mathrm{P}$` o `P` |
| Sigma | $\sigma$ o $\varsigma$ | `$\sigma$` o `$\varsigma$` | $\Sigma$ | `$\Sigma$` |
| Tau | $\tau$ | `$\tau$` | $\mathrm{T}$ | `$\mathrm{T}$` o `T` |
| Upsilon | $\upsilon$ | `$\upsilon$` | $\Upsilon$ | `$\Upsilon$` |
| Phi | $\phi$ o $\varphi$ | `$\phi$` o `$\varphi$` | $\Phi$ | `$\Phi$` |
| Chi | $\chi$ | `$\chi$` | $\mathrm{X}$ | `$\mathrm{X}$` o `X` |
| Psi | $\psi$ | `$\psi$` | $\Psi$ | `$\Psi$` |
| Omega | $\omega$ | `$\omega$` | $\Omega$ | `$\Omega$` |

---

## Código para vector en correlación

$(\delta_k, \lambda_k) \sim \mathcal{N} \left( \left( \begin{smallmatrix} 0 \\ 0 \end{smallmatrix} \right), \boldsymbol{\Sigma} \right)$ El código es `$(\delta_k, \lambda_k) \sim \mathcal{N} \left( \left( \begin{smallmatrix} 0 \\ 0 \end{smallmatrix} \right), \boldsymbol{\Sigma} \right)$`

