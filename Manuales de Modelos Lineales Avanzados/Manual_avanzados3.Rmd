---
title: "Manual para el curso Modelos Lineales Avanzados (XS-0125) Tercer Examen"
author: |
  | **Autor:** Anderson Martínez <br>
  | **Colaboradores:** Sebastián Calvo, Justin Zhu, Fabián Almirall
output:
  pdf_document:
    toc: true
    toc_depth: 3
    number_sections: true
  word_document:
    toc: true
    toc_depth: '3'
---

```{r, echo=FALSE, message=FALSE, warning=FALSE }
# 1. Cargar librerías necesarias
library(ggplot2)
library(dplyr)
library(lattice)
```



# Introducción a los modelos lineales generalizados

Todos los modelos vistos anteriormente en los cursos anteriores y en este, son una combinación lineal de $\beta's$ y $X's$, por esta razón es que todos estos modelos les colocamos el nombre de modelos lineales.

Los $\beta's$ y $X's$ que normalment escribrimos del modelo, le llamaremos la parte lineal del modelo, es decir la **parte lineal** en la modelación univariada *(la variables respuesta es una variable)* es igual a:


$$\sum_{j=0}^{p-1}{\beta_jX_j}$$


Además tenemos este nuevp elemento llamado la **función de enlance**, la cual es:

$$g(\cdot)$$


También sabemos que cada variable respuesta está asociada a una distribución, a esto le llamamos la **parte aleatoria** la cual se denota de la siguiente manera:


$$y|x\sim f(y|x)$$


La unión de los compenentes anteriores es lo que nos forma un modelo lineal generalizado:


$$g(\mu_{y|x}) =\sum_{j=0}^{p-1}{\beta_jX_j}$$

# Componentes de un modelo lineal generalizado (GLM)


+ Componente aleatoria: identifica la variable respuesta y su distribución de probabilidad.


+ Componente sistemática: especifica las variables explicativas (independientes o o predictoras) utilizadas en la función predictora lineal (una combinación lineal de las variables predictoras).


+ Función de enlace: es una función del valor esperado de $Y$, $E(Y)$ o $\mu_Y$, que relaciona la esperanza de la respuesta con las variables predictoras, es decir, la componente aleatoria con la sistemática. 


## Componente aleatorio
 

+ La componente aleatoria de un GLM consiste en una variable aleatoria Y con observaciones independientes $(y_1,…,y_n)$.


+ En muchas aplicaciones las observaciones son continuas y se puede asumir para $Y$ una distribución condicional normal.


+ En otros casos las observaciones de $Y$ son binarias, y se identifican como éxito y fracaso. Aunque de modo más general, cada $Y_i$ indicaría el número de éxitos de entre un número fijo de ensayos, y se modelizaría como una distribución condicional binomial.


+ En otras ocasiones cada observación es un conteo, con lo que se puede asignar a Y una distribución condicional de Poisson o una distribución condicional binomial negativa. 


+ Cuando la respuesta Y tiene una distribución asimétrica con valores continuos a partir de cero, se puede usar la distribución condicional gamma.

*Nota:* observe que en todos los casos se habla de la distribución condicional.

## Componente sistemático


+ El componente sistemático de un GLM especifica las variables explicativas, que entran en forma de efectos fijos en un modelo lineal, es decir, las variables $X$ se relacionan como:

$$\eta = \beta_1X_1+...+\beta_pX_p$$


+ Esta combinación lineal se denomina *predictor lineal* y se representa con h.

+ Algunos de los $\beta's$  en esta combinación son coeficientes de variables continuas.


+ Otros son coeficientes de factores. Recordando que en esos casos se crean variables auxiliares y cada una lleva un coeficiente.


+ También se pueden incluir interacciones entre dos o más factores, así como entre factores y variables continuas. 

+ Los parámetros del predictor lineal $\beta's$  son fijos para cualquier valor que tomen las $X's$. Sin embargo, los valores de esos parámetros son desconocidos y se deben estimar. Por lo tanto, para un vector de valores de las $X$ $(x_1,…,x_n)$, el correspondiente predictor lineal h es fijo (dados los coeficientes del modelo). 


## Función de enlace


+ La función de enlace especifica una función g(.) que relaciona  la esperanza de la respuesta mY con el predictor lineal h como:


+ La función g más simple es g(m)=m, esto es, la identidad que da lugar al modelo de regresión lineal clásico.


+ Entonces el modelo lineal clásico para respuestas continuas (normales) son un caso particular de un GLM con una función de enlace identidad.

 
+ Los GLM generalizan la regresión clásica de dos modos: 1) permitiendo que Y tenga distribuciones diferentes a la normal, 2) incluyendo distintas funciones de enlace.



*Notas:* 

1. En r un `glm` con `family = "normal" ` es lo mismo que un `lm`.

2. Sabemos `lmer` es para modelos lineales mixtos, en el caso de modelos generalizados mixtos es con `glmer`.

3. En este caso como algunos modelos generalizados no dependen de la función normal el supuesto de normalidad se cae.

# Respuesta binaria o binomial

## Función de enlace logit


$$\text{logit}(p) = \log\left(\frac{p}{1-p}\right) \quad \text{para } p \in (0, 1)$$


Si tenemos una distribución de forma que $y|x \sim Bernoulli(\pi_x)$, esto nos lleva que el valor esperado **condicional** $E[y|x]=\pi_x = \mu_{y|x}$

El modelo lineal generalizado para este caso que tenga un factor con tres niveles queda de la siguiente manera:


*Tenemos que la parte lineal usando el modelo de suma nula es:*

$$\beta_0+\beta_1X_1+\beta_2X_2$$

Este modelo tiene la restricción de que $\beta_3 = -\beta_1-\beta_2$


*Tenemos que la función de enlace es:*


$$\log\left(\frac{\pi_j}{1-\pi_j}\right)$$

Note que $\pi$ depende del j-ésimo tratamiento


*Uniendo ambos compentes obtenemos el siguiente resultado:*

$$\log\left(\frac{\pi_j}{1-\pi_j}\right)=\beta_0+\beta_1X_1+\beta_2X_2$$


Esto nos dice que la probabilidad en el tratamiento j-ésimo se modela de la forma.


Para despejar esa probabilidad lo que hacemos es que colocamops una exponencial

$$\left(\frac{\pi_j}{1-\pi_j}\right)= e^{PL}$$

Despejamos el $\pi_j$ y esto nos lleva a que:


$$\pi_j= \frac{e^{PL}}{1+e^{PL}}$$

donde:

+ $PL = \beta_0+\beta_1X_1+\beta_2X_2$, lo cual corresponde a la parte lineal

Observe que

$$\pi_j = \begin{cases} 0.5 & \text{si } PL = 0 \\ >0.5 & \text{si } PL > 0 \\ <0.5 & \text{si } PL < 0 \end{cases}$$


$$\lim_{PL \to \infty} \pi_j = 1 \quad \text{y} \quad \lim_{PL \to -\infty} \pi_j = 0$$


*Nota:* La parte lineal no tiene niguna interpretación

## Función de enlace Poisson

$$\log(\lambda_j)=\beta_0+\tau_j$$

$$\lambda_j=e^{PL}$$


$$\lim_{PL \to \infty} \lambda_j = 1 \quad \text{y} \quad \lim_{PL \to -\infty} \lambda_j = 0$$

## Propensión

Sabemos que la propensión la podemos obtener como:

$$odds = \left(\frac{p}{1-p}\right)$$

## Análisis de Hiberbicidas

En un experimento se aplican 3 dosis de un herbicida (200, 400 y 800 g/l) alrededor de unas plantas y se tiene un grupo control al que no se le aplicó nada (dosis=0). Se cuantifica el daño en las plantas tomando 
20 hojas de cada planta y se cuenta el número de hojas que presenta daños a causa del herbicida. Se quiere saber si hay un efecto de la dosis en el daño en las hojas.

*Note que en este caso la variables tiene una distribución binomial, esto nos lleva a que:*

$$Y|\text{Dosis}\sim Binomial(n = 20,\pi_{\text{Dosis}})$$
Para esta situación la esperanza es:

$$\mu_j = n\pi_j$$

```{r}
base=read.csv("herbicidas.csv",row.names=NULL,header=T,sep=";")
base$genotipo=factor(base$genotipo)
base$dosix=factor(base$dosis)
prop=base$mal/20
boxplot(prop~dosix,ylab="proporción de dano",xlab="dosis",data=base)
```


En el gráfico se observa que la proporción de daños tiende a ser mayor para las dosis 200, 400 y 800 con respecto al control, y entre ellas es un poco mayor para la dosis 800.


### Ejemplo de propensión (odds)


Si $p = 0.6$, esto nos lleva a que:

$$odds = \left(\frac{0.6}{0.4}\right) = \frac{3}{2}$$

Esto se interpreta como:

Por 3 hojas dañadas, existen 2 hojas no dañadas. Cuando el $odds=1$, quiere decir que $P = 0.5$.


*Notas:*

1. El logaritmo de los odds debe ser lineal, pero si la dosis es un factor no es necesaria la linealidad.

2. Una ventaja que tiene usar los datos desagrupados (por ejemplo que en este experimento se tuvieran la información de cada hoja en específico), es medir ciertas variables específicas para cada unidad, un ejemplo de ello sería el tamaño o diamétro de la hoja.



### Estimación de un modelo Binomial en r

```{r}
mod.trat=glm(cbind(mal,20-mal)~dosix,family=binomial(link = "logit"),data=base)
mod.trat$coef
```

**La ecuación estimada usando como referencia la dosis 0 está dada por:**

$$\log\left(\frac{p}{1-p}\right)=-2,43+2,15D_{200}+2,29D_{400}+3,05D_{800}$$


## Estimación de la parte lineal con suma nula 

```{r}
options(contrasts=c("contr.sum","contr.poly"))
contrasts(base$dosix)
```

	
```{r}
mod.sum=glm(cbind(mal,20-mal)~dosix,family=binomial,data=base)
# Se coloca cbind(mal,20-mal), porque los datos no están agrupados
```

```{r}
b = mod.sum$coef
tau_4 = -sum(b[-1])
pl=(b[1]+ c(b[-1], tau_4))
names(pl)=c("dosix0","dosix200","dosix400","dosi800")
pl
```



### Estimar la probabilidad de éxito en un modelo binomial

Para este ejemplo nuestro éxito es que la hoja esté dañanda, por lo que a continuación se le presenta, la forma de cálcularlo:


```{r}
dt=mod.trat$coef
beta0=dt[1]
taus=dt
taus[1]=0
p1=exp(beta0+taus)/(1+exp(beta0+taus))
names(p1)=c("D0","D200","D400","D800")
p1
```

La interpretación del `D200` es:

Para la dosis 200 se estima por cada 100 hojas en promedio 46 jojas están dañadas.

### Estimación del odds

```{r}
round(p1/(1-p1),2)
```
La interpretación del `D200` es:

Para la dosis 200, se estima que en promedio por cada 75 hojas dañadas existen 100 no dañadas.

### Estimación automática con predict()

Directamente con la función de R se obtiene una estimación para cada planta, pero hay 13 plantas con el mismo tratamiento por lo que no es necesario tener tantas estimaciones, se puede saca un valor único por tratamiento.

```{r}
# Si se coloca solo predict devuelve la parte lineal
probs=predict(mod.trat,type="response")
p2=tapply(probs,base$dosix,mean)
names(p2)=names(p1)
p2
```

Otra forma de sacarlo

```{r}
p3 = unique(predict(mod.trat,type="response"))
names(p3) = names(p2)
p3
```



### Estimar la probabilidad para una cantidad específica de unidades

Se quiere estimar promedio de hojas dañadas por planta en cada tratamiento, si la planta tiene 1500 hojas


```{r}
p1=exp(beta0+taus)/(1+exp(beta0+taus))
1500*p1
```

### Prueba para verificar si la dosis tiene un efecto en la probabilidad


```{r}
drop1(mod.trat,test="LRT")
```

Con la prueba de la Razón de Verosimilitud (LRT) se obtiene una deviancia para el modelo grande de 68.55, mientras que para el modelo en que se elimina dosis se obtiene 276.66. La diferencia entre ellas es 208.11, que proviene de una distribución chi-cuadrado con 3 grados de libertad, y tiene una probabilidad asociada de error tipo I diminuta (p<0.001). Por lo tanto, se rechaza la hipótesis de que ambos modelos explican lo mismo. Finalmente, se dice que sí existe un efecto de la dosis sobre la probabilidad de daño, es decir, no todas las dosis tienen las misma probabilidad de daño.


### Comparción de propensiones


Primero hay que hacer los contrastes. Si los coeficientes para las dosis se
denominan con $\alpha_{200}$,  $\alpha_{400}$ y  $\alpha_{800}$, se sabe que $\alpha_0 = 0$. Por ejemplo para comparar
la dosis 800 contra la dosis 400, se tiene que el OR respectivo es:

Por lo tanto, el vector de coeficientes del contraste asociado está dado por: c(0,0,-1,1). De esta forma se construyen los otros constrastes y se colocan en una matriz con los contrastes. Si cada contraste se denomina $\eta$, se tiene que** $OR = \exp(\eta)$

$$OR = \exp(\alpha_{a800}-\alpha_{400})$$


La demoestración de este proceso es:

$$\log\left(\frac{\pi_{800}}{1-\pi_{800}}\right)-\log\left(\frac{\pi_{400}}{1-\pi_{400}}\right)$$

Lo expoenciamos y por propiedades de logatimos nos lleva a este resultado:


$$\left(\frac{\frac{\pi_{800}}{1-\pi_{800}}}{\frac{\pi_{400}}{1-\pi_{800}}}\right)=e^{(\beta_0+\tau_{800})-(\beta_0+\tau_{400})}$$

Este proceso anterior es lo que llamamos *Odds Ratio (OR)*, que es lo mismo que decir la razón de dos propensiones.

**Propiedad:**


$$\text{Si } \pi_i = \pi_j \iff OR_{i|j}=1 \iff \tau_j = \tau_i$$




Los vectores se contrutyen de la siguiente manera:


```{r}
dt=mod.trat$coef
d0 = c(1,0,0,0)
d200 = c(1,1,0,0)
d400 = c(1,0,1,0)
d800 = c(1,0,0,1)
d0.200 = d0-d200
d0.400 = d0-d400
d0.800 = d0-d800
d200.400 = d200-d400
d200.800 = d200-d800
d400.800 = d400-d800
h = cbind(d0.200, d0.400, d0.800, d200.400, d200.800, d400.800 )
eta=t(h)%*%dt
or=exp(abs(eta))
colnames(or)="OR"
round(or,2)
```


**Ojo que estos son los contrastes**

```{r}
c1=c(0,0,-1,1)
c2=c(0,-1,0,1)
c3=c(0,0,0,1)
c4=c(0,-1,1,0)
c5=c(0,0,1,0)
c6=c(0,1,0,0)
h=cbind(c1,c2,c3,c4,c5,c6)
eta=t(h)%*%dt
or=exp(eta)
rownames(or)=c("800-400","800-200","800-0","400-200","400-0","200-0")
colnames(or)="OR"
round(or,2)
```


Con la estimación puntual de los OR, se observa que son muy altos al comparar todas las dosis contra el control, pero entre las otras dosis (200, 400 y 800), las que más se diferencian son 800 vs 200 y luego 800 vs 400.

*Prueba de hipótesis*

Tenemos lo siguiente:

$$
\begin{aligned}
\eta &= \alpha_i - \alpha_j \\
Var(\eta) &= Var(\alpha_i - \alpha_j) \\
&= Var(\alpha_i) + Var(\alpha_j) \\
 ee(\eta)&= \sqrt{Var(\alpha_i) + Var(\alpha_j)} 
\end{aligned}
$$

Esto es equivalente a :

$$\sqrt{e^{\alpha_i - \alpha_j}}$$



```{r}
ee=sqrt(diag(t(h)%*%vcov(mod.trat)%*%h))
(q=eta/ee)
```

```{r}
(pn=pnorm(q, lower.tail = F))
pn < 0.05/12 # Se usa bomferroni
# Observe que es una prueba de dos colas 2*d = 12, donde d = 6
```
**Cálculo de Intervalos de Confianza**


$$IC = e^{\eta\pm z\cdot ee}$$

```{r}
k=5 #número de intervalos de confianza
qz=qnorm(1-0.05/(2*k))
ic=cbind(exp(eta-qz*ee),exp(eta+qz*ee))[-4,] #Quitamos 4 porque no es significativa
rownames(ic)=rownames(or)[-4] #Quitamos 4 porque no es significativa
ic1=cbind(ic,ee[-4]) #Quitamos 4 porque no es significativa
colnames(ic1)=c("L.Inf","L.Sup","ee")
ic1
```
Las comparaciones contra la dosis 0 son muy imprecisas, en cambio las comparaciones entre dosis 800 y las otras dos dosis son más precisas. Esto es una consecuencia de que estas dos últimas comparaciones tienen errores estándar más pequeños.


*Nota:* Las probabilidades no se dividen debido a su complejidad matemática, para calcular sus intervalos de confianza, en estadística Bayesiana esos intervalos se pueden hacer de forma más sencilla.

**Interpretación y cálculo de los límites inferiores de las comparaciones**

```{r}
qz=qnorm(1-0.05/k)
LIM=exp(eta-qz*ee)[-4]
names(LIM)=rownames(or)[-4]
LIM
```
Con una confianza global de 95%, se puede decir que la propensión de daño para D800 es al menos 40% mayor que la de D400, 61% mayor que la de D200 y es 11.5 veces la de D0, que la propensión de daño de D400 es al menos 5.4 veces la de D0 y la propensión de daño de D200 es al menos 4.7 veces la de D0.

# Análisis de la dosis como continua

Cuando se utliza la variable como continua, podemos realizar interpolaciones para diferentes rangos de una variable en específico.

```{r, warning=FALSE}
library(car)
logodds=log((prop)/(1-prop))
scatterplot(logodds~log(base$dosis+0.1,2),xlab="dosis",ylab="log(odds)")
```


En el gráfico se observa que para el intervalo que va de 0 a 200, hay un comportamiento diferente al resto del intervalo estudiado, lo cual hace pensar que la relación general entre el log(odds) y la dosis no es lineal. Si se observa solo la parte de la dosis que va de 200 en adelante, se aprecia un comportamiento más lineal. Entonces, el modelo que se ajusta con la variable dosis en forma continua no está siguiendo lo que se observa en este gráfico.

El problema de la estimación es como se prueba la linealidad, debido a que hay valores únicos, es decir que hay sujetos que no tiene esa variable continua exacytamente igual. Po ejmplo si se quiere hacer un estudio de la relación de la altura con ser hombres, en donde en la muestra se pueden tener una cantidad de sujetos, pero prácticamemte todos tiene una altura distinta asociada a una variable binaria. En algunos casos si se cumple, en otros este problema puede generarnos problema en el supuesto de linelidad.

**En este caso, como no hay linealidad no es recomendable asumir linealidad, por lo que se tiene que considerar como factor o realizar una transformación**

## Estimación del modelo con la variable continua


```{r}
# Se le suma uno al log porque hay valores que son 0 
# Además como la dosis se está duplicando, se hace el logaritmo en base 2
mod2=glm(cbind(mal,20-mal)~log(dosis+1,2),family=binomial,data=base)
(beta=mod2$coef)
```
**Primero tenemos que probar la siguiente hipótesis:**

$$H_0:\beta_1=0$$
```{r}
drop1(mod2, test = "Chisq")
```

Debido a que la probabilidad asociada a la prueba es p < 0.001, podemos decir que al duplicar la dosis la propensión de daño cambia significativamente.

*Nota:* Recordar que si no se rechaza, se tiene que analizar si tengo una muestra suficiente para probar que hayan diferencias (Potencia). Es importante tomar en cuentra que para regresión logística se necesitan muchos datos.

Tenemos que:

$$
\begin{aligned}
&Odds_2 = e^{-2.54+0.30\cdot\log_2{(2D)}}\\
&Odds_2 = e^{-2.54+0.30\cdot(\log_2{(2)}+\log_2{(D)})}\quad\text{Note que} \log_2{(2)} = 1\\
&Odds_1 = e^{-2.54+0.30\cdot(\log_2{(D)})}\\
&\implies OR_{2|1} = e^{0.30} = 1.35
\end{aligned}
$$

**Interpretación:** Al duplicar la dosis la propensión se amplifica ó simplifica 1.35 veces, es decir aumenta 35%.

Luego realizamos su intervalos de confianza

```{r}
exp(confint(mod2))
```

Con un 95% de confianza, al duplicar la dosis la propensión de daño se multiplica entre 28% y 43%.


*La ecuación estimada usando dosis de forma continua es la siguiente:*

$$\log\left(\frac{p}{1-p}\right)=-1.41+0,003D$$

```{r}
xh=c(1,300)
eta=t(xh)%*%beta
prob300.a=exp(eta)/(1+exp(eta))
prob300.b=predict(mod2,data.frame(dosis=300),type="response")
c(prob300.a,prob300.b)
```

*En el siguiente gráfico se muestren los porcentajes de hojas dañadas por planta contra la dosis*

```{r}
curve(exp(beta[1]+beta[2]*x)/(1+exp(beta[1]+beta[2]*x)),0,2000,
ylab="probabilidad",xlab="dosis",ylim=c(0,1))
points(base$dosis,base$mal/20,pch=18,col=2)
points(300,prob300.a,col=4,pch="+",cex=1.5)
a=round(beta[1],2); b=format(round(beta[2],3),scientific=F)
title(main=(bquote(log(pi/(1-pi))==.(a)+.(b)*D)))
```





# Análisis de linealidad en estudios longitudinales

*Observe el siguiente ejemplo visto en clase*

Un estudio busca analizar si los cambios en el rendimiento físico se encuentran relacionados con los cambios en el rendimiento cognitivo de las personas conforme estas envejecen. Para esto se han recolectado datos que provienen del proyecto “Comparación del envejecimiento físico y cognitivo en personas de los Estados Unidos de América y de Costa Rica”. Cada sujeto de investigación se presentó tres veces a realizar las pruebas (físicas y neurocognitivas), en diferentes años. Interesa conocer si cambios en el rendimiento físico están asociados a cambios en el rendimiento cognitivo tanto en hombres como en mujeres y si esta asociación es más fuerte en un sexo que en el otro.  

Basado en este estudio se hace un gráfico donde se aprecie una interacción entre rendimiento cognitivo y el rendimiento físico 


```{r, echo=FALSE}
# Configurar semilla para reproducibilidad
set.seed(2026)

# 2. Simulación de los datos longitudinales
n_sujetos <- 100
mediciones <- 3

# Crear estructura básica de los sujetos
datos_sujetos <- data.frame(
  id = rep(1:n_sujetos, each = mediciones),
  sexo = rep(sample(c("Hombre", "Mujer"), n_sujetos, replace = TRUE), each = mediciones),
  tiempo = rep(1:mediciones, n_sujetos)
)

# Simular variable independiente (Rendimiento Físico) con variabilidad intra-sujeto
datos_sujetos <- datos_sujetos %>%
  mutate(
    rend_fisico = rnorm(n(), mean = 50, sd = 10) - (tiempo * 2) # Tiende a bajar con el tiempo
  )

# Simular variable dependiente (Rendimiento Cognitivo) metiendo el efecto de INTERACCIÓN
# Efecto hipotético: En mujeres, la caída del rendimiento físico afecta más al cognitivo (mayor pendiente).
datos_sujetos <- datos_sujetos %>%
  mutate(
    error = rnorm(n(), mean = 0, sd = 5),
    rend_cognitivo = case_when(
      sexo == "Mujer" ~ 30 + 0.8 * rend_fisico + error,  # Pendiente fuerte (0.8)
      sexo == "Hombre" ~ 45 + 0.3 * rend_fisico + error  # Pendiente más leve (0.3)
    )
  )

# 3. Construcción del Gráfico de Interacción
ggplot(datos_sujetos, aes(x = rend_fisico, y = rend_cognitivo, color = sexo, fill = sexo)) +
  # Rectas de regresión ajustadas para evidenciar la interacción
  geom_smooth(method = "lm", formula = y ~ x, se = TRUE, size = 1.2) +
  # Personalización estética y etiquetas
  scale_color_manual(values = c("Hombre" = "#1f77b4", "Mujer" = "#d62728")) +
  scale_fill_manual(values = c("Hombre" = "#1f77b4", "Mujer" = "#d62728")) +
  theme_minimal(base_size = 13) +
  labs(
    title = "Interacción entre Rendimiento Físico y Cognitivo según el Sexo",
    x = "Cambios en el Rendimiento Físico ",
    y = "Cambios en el Rendimiento Cognitivo ",
    color = "Sexo",
    fill = "Sexo"
  ) +
  theme(
    plot.title = element_text(face = "bold", hjust = 0.5),
    plot.subtitle = element_text(hjust = 0.5, color = "gray40"),
    legend.position = "bottom"
  )
```

Luego se gráfica la pendiente de cada persona 


```{r}
xyplot(rend_cognitivo~rend_fisico|id, groups = sexo, type = c("r", "p") , auto.key = list(columns = 2),data = datos_sujetos[1:30,], pch = 18)
```

Observe que con tres medidas es muy díficil aceptar el supuesto de linealidad

*A continuación se le demuestra por medio de una simulación lo que está ocurriendo*

Tenemos el siguiente modelo con sus valores verdaderos

$$\mu = 2+3X$$
Además se tiene que $\hat{\sigma} = 1$

*A continuación se simulan con dos semillas distintas*

```{r, warning=FALSE}
set.seed(1234)
x = runif(3,2,2.5)
y = 2+ 3*x +rnorm(2,0,1)
lm(y~x)$coef[2]
```
```{r, warning=FALSE}
set.seed(1236)
x = runif(3,2,2.5)
y = 2+ 3*x +rnorm(2,0,1)
lm(y~x)$coef[2]
```

Podemos observar que las pendientes dieron un resultado muy distinto

Ahora vamos a realizarlo con `x = runif(3,2,2.01)`.

```{r, warning=FALSE}
set.seed(1234)
x = runif(3,2,2.01)
y = 2+ 3*x +rnorm(2,0,1)
lm(y~x)$coef[2]
```
```{r, warning=FALSE}
set.seed(1236)
x = runif(3,2,2.01)
y = 2+ 3*x +rnorm(2,0,1)
lm(y~x)$coef[2]
```


Es importante notar que cuando bajamos la x, el problema se empeoró y ahora las pendientes estimadas son demasiado distintas.


Para un último caso, lo haremos con `x = runif(3,2,20)`

```{r, warning=FALSE}
set.seed(1234)
x = runif(3,2,20)
y = 2+ 3*x +rnorm(2,0,1)
lm(y~x)$coef[2]
```

```{r, warning=FALSE}
set.seed(1236)
x = runif(3,2,20)
y = 2+ 3*x +rnorm(2,0,1)
lm(y~x)$coef[2]
```


En este caso las pendientes son mucho más parecidas. Esto nos lleva a que no siempre es bueno realizar estudios longitudinales, si el tiempo o nuestyra variable x no está causando ningún y que sería mejor capturar una mayor cantidad de datos en un solo momento, para obtener mejores resultados. Si el objetivo del estudio verdaderamente hacer un estudio longitinal, lo que se recomienda es que haya más tiempo entre cada medición, para observar un efecto.


# Modelos para conteos


La diferencia entre un modelo binomial y otro poisson radica en la información que se tiene, esto es porque en un modelo binomial yo puedo conocer o conozco el máximo en cambio en el poisson esto no es así.Un ejemplo de esto con el experimento de las hojas dañadas es que en ese experimento sabemos que nuestra variable resúesta es el número de hojas dañadas y sabiamos que el total o el máximo de hojas de cada planta eran 20, por lo que yo puedo calcular la proporción de hojas dañadas de este total. Para el caso de la poisson es como si yo tomará una ramita para contar las hojas dañanas, pero no se posee conocimiento, ni se puede averiguar de cual puede ser el número máximo de hojas dañadas.

Un modelo Poisson se escribe de la siguiente manera:

$$\log(\lambda_x) = \beta_0+\beta_1X$$


donde:

+ $\lambda_x=E[X|Y]$


+ $E[Y|X] = Var[Y|X]$ y $Y|X\sim P(\lambda_x)$


*En un modelo binomial tenemos esto*


$$\frac{odds_{x+1}}{odds_x}=\frac{\frac{\pi_{x+1}}{1-\pi_{x+1}}}{\frac{\pi_{x}}{1-\pi_{x}}}=2$$


*En un modelo Poisson*


Al aumentar x en una unidad, la propensión es 2 veces mayor o la propensión de daño es el doble al aumentar la x una unidad.

$$\frac{\lambda_{x+1}}{\lambda_x}=2$$



Al aumentar x en una unidad, el promedio es 2 veces mayor

En este tipo de modelo no tiene sentido probar el supuesto de homocedasticidad, debido a que la media es igual a la varianza. Debido a que este modelo se distribuye como una Poisson, no se necesita el supuesto de normaalidad. El único supuesto a tomar en cuenta puede ser la multicolinealidad, pero este depende de la cantidad de factores.



**Experimento de Moscas**

En un experimento diseñado para ver el tipo de aditivo que se puede poner en una trampa para moscas, se probaron 4 tratamientos: miel, sirope, leche y vinagre.

En diferentes lugares donde había una población importante de moscas se colocaron 4 trampas en cada lugar, una con cada aditivo y se contó el número de moscas atrapadas en cada trampa. 

El experimento se repitió durante varios días. Se usaron dos lugares y se repitió durante 4 días, por lo que se tienen 32 observaciones de los conteos para las 4 trampas.

*Notas: Ese experimento se podría hacer más rápido si se hacían 16 trampas en un solo día*



*A continuación se ve el gráfico de un conteo*

```{r, message=FALSE, warning=FALSE}
load("moscas.Rdata")
attach(base)
library(ggplot2)
qplot(trat1,conteo,geom="boxplot",ylab="conteo",xlab="tratamiento")
```
*Nota: Recuerde que la homocedasticidad se omite*

En este caso se cuenta el número de moscas en un momento y lugar específico bajo un cierto tratamiento. Este número teóricamente puede ir de cero a infinito, siendo en general los conteos obtenidos números no muy grandes, por lo que la distribución es genuinamente Poisson y no convendría aproximarla a la normal por el hecho de no tener conteos muy grandes.


**El modelo es:**

$$\log(\lambda_j)=\beta_0+\tau_j$$

Con la restricción de $\sum\tau_j = 0$


## Estimación de un modelo Poisson

```{r}
options(contrasts=c("contr.sum","contr.poly")) # Suma nula
mod1=glm(conteo~trat1,family=poisson,data=base)
```



```{r}
contrasts(trat1)
```

**Estimación de todos los coeficientes del modelo**

```{r}
d=mod1$coef
d1=c(d,-sum(d[2:4]))
names(d1)[5] = "trat14"
round(d1,2)
```

## Cálculo de valores ajustados de un modelo Poisson

### Forma manual


$$\text{Si} \log(\lambda_j)=\beta_0+\tau_j\implies\lambda_j = e^{\beta_0+\tau_j}$$


```{r}
d2=d1[-1]
lambda=exp(d[1]+d2)
names(lambda)=levels(trat1)
round(lambda,2)
```


### Forma automática.


```{r}
fit=predict(mod1,type="response")
means = unique(predict(mod1,type="response"))
names(means) = levels(base$trat1)
# También se puede con
# tapply(fit,trat1,mean) 
means
```

## Supuesto de un modelo Poisson

En este tipo de modelo tenemos un supuesto muy importante:

$$\lambda_j=\sigma_j$$

$$E[Y|j]= V[Y|j]$$

Si este no se cumple existen dos alternativas, las cuales son:

+ Quasi-Poisson

+ Binomial Negativa


Además de esto se **tienen** que cumplir los siguientes supuestos:


1. Linealiadad con las predictoras continuas.

2. No multicolinealidad (Si en nuestro modelo hay más de una predictora númerica).



### Cálculo de los residuales normales


```{r}
r=residuals(mod1,type="response") # El type response, es una predicción de la media del modelo
#Sin el "response", calcula solo la parte lineal no la media
```

### Gráfico para visualizar los residuales


*A continuación se le presenta el código poara generar un gráfico que ayude a visualizar está situación*

```{r, eval=FALSE}
# El residual al cuadrado es una medida de variabilidad
plot(log(r^2)~log(fit),pch=18,xlab=expression(log(hat(lambda))),
ylab=expression(log(r^2)))
abline(0,1) #Esto es la identidad
```


```{r, echo=FALSE}
# Configurar la ventana para ver los 3 gráficos lado a lado
par(mfrow = c(1, 3))

# Valores fijos de log(fit) basados en tu imagen
log_fit <- c(rep(0.3, 3), rep(1.45, 6), rep(2.05, 6), rep(2.23, 6))

# -------------------------------------------------------------------------
# 1. EQUIDISPERSIÓN (Puntos perfectamente alrededor de la línea)
# -------------------------------------------------------------------------
# Definimos desvíos fijos y balanceados para que fuercen la oscilación sobre la línea
desvios_equi <- c(
  -0.3, 0.0, 0.3,                 # Para x = 0.3
  -0.4, -0.1, 0.2, -0.2, 0.1, 0.4, # Para x = 1.45
  -0.5, -0.2, 0.1, -0.1, 0.3, 0.4, # Para x = 2.05
  -0.4, -0.2, 0.0, 0.2, 0.3, -0.1  # Para x = 2.23
)
log_r2_equi <- log_fit + desvios_equi

plot(log_r2_equi ~ log_fit, pch = 18, cex = 1.5,
     xlim = c(0.2, 2.4), ylim = c(-1, 6),
     xlab = expression(log(hat(lambda))), ylab = expression(log(r^2)),
     main = "Equidispersión")
abline(0, 1, lwd = 2, col = "red")

# -------------------------------------------------------------------------
# 2. SOBREDISPERSIÓN (Todos los puntos arriba de la línea)
# -------------------------------------------------------------------------
# Sumamos valores positivos altos para desplazar toda la nube hacia arriba
desvios_sobre <- c(
  0.8, 1.2, 1.5,
  1.0, 1.4, 1.9, 2.3, 2.7, 3.1,
  1.2, 1.8, 2.4, 3.0, 3.5, 4.0,
  1.5, 2.0, 2.5, 3.1, 3.6, 4.2
)
log_r2_sobre <- log_fit + desvios_sobre

plot(log_r2_sobre ~ log_fit, pch = 18, cex = 1.5,
     xlim = c(0.2, 2.4), ylim = c(-1, 6),
     xlab = expression(log(hat(lambda))), ylab = expression(log(r^2)),
     main = "Sobredispersión")
abline(0, 1, lwd = 2, col = "red")

# -------------------------------------------------------------------------
# 3. SUBDISPERSIÓN (Todos los puntos abajo de la línea)
# -------------------------------------------------------------------------
# Restamos valores para arrastrar absolutamente todos los puntos debajo de la línea
desvios_sub <- c(
  -0.8, -1.0, -1.2,
  -0.8, -1.0, -1.1, -1.3, -1.4, -1.5,
  -0.9, -1.1, -1.3, -1.4, -1.6, -1.8,
  -1.0, -1.2, -1.3, -1.5, -1.7, -1.9
)
log_r2_sub <- log_fit + desvios_sub

plot(log_r2_sub ~ log_fit, pch = 18, cex = 1.5,
     xlim = c(0.2, 2.4), ylim = c(-1, 6),
     xlab = expression(log(hat(lambda))), ylab = expression(log(r^2)),
     main = "Subdispersión")
abline(0, 1, lwd = 2, col = "red")

# Restaurar ventana gráfica
par(mfrow = c(1, 1))
```


*Lo más común es observar Sobredispersión en muchos casos la subdispersión no sucede.*



### Cálculo de los residuales de Pearson

Un residual de Pearson se define como:

$$r_p=\frac{y-\hat{y}}{\sqrt{\hat{y}}}$$
De esto podemos obtener el cuadrado medio residual de Pearson el cual se define como:

$$\frac{\sum{r_p^2}}{n-p} = \phi$$
donde:

+ $\phi:$ Es el paramétro de dispersión

+ $n:$ El número de observaciones

+ $p$: número de paramétros del modelo

Para $\phi$, tenemos tres casos:

$$
\begin{aligned}
\phi \sim 1 &\implies \text{Equidispersión} \\
\phi > 1    &\implies \text{Sobredispersión} \\
\phi < 1    &\implies \text{Subdispersión}
\end{aligned}
$$

*A continuación se le presenta la fórmula de calcularlo en r*

```{r}
n = nrow(base)#número de observaciones
p = length(mod1$coef)#Número de coef
phi=sum(residuals(mod1,type="pearson")^2)/(n-p)
round(phi,1)
```
Se obtiene un parámetro de dispersión $\phi = 6.1$ , el cual es mucho mayor que 1, por lo que se comprueba la sospecha de que existe sobredispersión, por lo tanto, no se cumple el supuesto que dice que la media condicional es igual que la variancia condicional.

En ocasiones decir que la media es igual a la varianza es algo muy fantasioso e irreal, por lo que hay varias casos que pueden ocurrir uno de ello es cuando la varianza es proporcional a la media, con esto nos referimos a los siguiente:

*Esto lo podemos considerar como un supuesto*. Por lo que ignoramos un supuesto para incluir otro. 


$$\sigma_j^2=\phi\lambda_j$$


En el ejemplo anterior como $\phi = 6.1$

Esto nos lleva a que:

$$\begin{aligned}
&\lambda_1 = 2 \implies\sigma_1^2 = 12.2 \\
&\lambda_2 = 4 \implies\sigma_2^2 = 24.4 \\
\end{aligned}$$



Hay otros dos casos los cuales son:

1. Cuando la varianza tiene un comportamiento cuadrático, lo que sería una binomial negativa.


2. Cuando la varianza tiene una relación aleatoria con la media y no se puede observar su comportamiento. Por está razón en muchas ocasiones se utiliza la que no está tan aleajada.

```{r tablas-comparativas, echo=FALSE}
library(knitr)

# Datos Escenario 1
escenario1 <- data.frame(
  Rendimiento = c("Mejor", "Intermedio", "Peor"),
  Modelo = c("Binomial Negativa", "Quasi-Poisson", "Regresión Poisson"),
  Diagnostico = c("Ajusta la varianza de forma cuadrática.", 
                  "Corrige errores estándar usando phi.", 
                  "Subestima errores estándar (falsos positivos).")
)

# Datos Escenario 2
escenario2 <- data.frame(
  Rendimiento = c("Mejor", "Intermedio", "Peor"),
  Modelo = c("Binomial Negativa", "Regresión Poisson", "Quasi-Poisson"),
  Diagnostico = c("Sigue siendo el más flexible y robusto.", 
                  "Eficiente y parsimonioso si phi es cercano a 1.", 
                  "Introduce sobreparametrización innecesaria.")
)

# Renderizar tablas
kable(escenario1, caption = "Jerarquía con Alta Sobredispersión")
kable(escenario2, caption = "Jerarquía con Baja Dispersión / Muestra Pequeña")
```

*A continuación se le presenta como colocar el modelo quasipoisson en r*


```{r}
mod2=glm(conteo~trat1,family=quasipoisson(link=log),base)#da lo mismo sin el (link=log)
summary(mod2)
```

```{r}
ee2=summary(mod2)$coef[,2]
ee1=summary(mod1)$coef[,2]
ee2/ee1 
```

Todos los errores estándar se inflan. **Siempre** la Quasi-Poisson va a tener errores estándar mayores que la Poisson normal.

```{r}
sqrt(phi)
```

Se puede observar que los coeficientes son los mismos obtenidos anteriormente pero los errores estándar ahora son mayores. Estos son los correctos. El error estándar de cada coeficiente ahora es $\sqrt{\phi}$ veces el que se había obtenido antes.

*A continuación observe la diferencia entre los $\phi$ de los modelos*


```{r}
summary(mod1)$disp # Modelo de equidispersión
```


```{r}
summary(mod2)$disp #Modelo de sobredispersión
```


### Análisis de si la varianza es proporcional a la media

```{r}
med=tapply(conteo,trat1,mean)
v=tapply(conteo,trat1,var)
round(v/med,2)
```

En el caso de sirope la variancia es 10.2 veces la media, mientras que en el vinagre la variancia no llega a ser ni el doble de la media. Por lo tanto, no parece que las variancias sean proporcionales a las medias.

*Nota:Si tenemos un predictor que es una variable continua, se necesitan más datos para hacer la verificación de que es proporcional*

## Comparación de medias


Tenemos las siguientes medias:


$$
\begin{aligned}
\hat{\lambda_1}=e^{\beta_0+\tau_1}\\
\hat{\lambda_2}=e^{\beta_0+\tau_2}
\end{aligned}
$$



Para compararlas se debe realizar una razón:

$$\text{Razón}=e^{\tau_2-\tau_1}$$

Esto nos lleva a la siguiente hipótesis:

$$H_0: \lambda_2 = \lambda_1  \iff \tau_2=\tau_1$$
*Nota: Recuerde que la interpretación es aumenta x veces o es el doble etc.*


# Poisson inflada con ceros

Esto es un fenoméno de cuando la variable que yo estoy estudiando tiene  muchos ceros. Su nombre en inglés es *Zero-inflation Poisson*


```{r simulacion-zip, message=FALSE, warning=FALSE, fig.width=8, fig.height=5, echo=FALSE}
library(ggplot2)

set.seed(2026)
n <- 500
omega <- 0.35  # 35% de probabilidad de obtener un cero estructural
lambda <- 4    # Media del componente Poisson

# 1. Simulación del proceso de mezcla
# Generamos una variable Bernoulli (0 o 1) para decidir el origen del dato
es_cero_estructural <- rbinom(n, size = 1, prob = omega)
datos_poisson <- rpois(n, lambda = lambda)

# Si es_cero_estructural es 1, el conteo es 0; si no, viene de la Poisson
contingencias <- ifelse(es_cero_estructural == 1, 0, datos_poisson)

df_zip <- data.frame(Conteos = contingencias)

# 2. Gráfico de Densidad / Histograma
ggplot(df_zip, aes(x = Conteos)) +
  # Usamos un histograma de densidad (y = ..density..) adaptado a datos discretos
  geom_histogram(aes(y = ..density..), binwidth = 0.5, 
                 fill = "steelblue", color = "white", alpha = 0.8) +
  # Línea de densidad suavizada para marcar el perfil de la distribución
  geom_density(color = "darkred", size = 1, adjust = 2) +
  labs(
    title = "Distribucion Poisson Inflada con Ceros (ZIP)",
    subtitle = paste0("n = ", n, ", ", omega*100, "% de ceros estructurales y lambda = ", lambda),
    x = "Conteo de eventos (Y)",
    y = "Densidad de probabilidad"
  ) +
  scale_x_continuous(breaks = 0:max(contingencias)) +
  theme_minimal() +
  theme(
    plot.title = element_text(face = "bold", size = 14),
    panel.grid.minor = element_blank()
  )
```
## Diferencia entre el modelo Quasipoisson, Binomial Negativa y Poisson al momento de probar factores fijos

Para la siguiente hipótesis:

$$H_0: \lambda_1 = ... = \lambda_k $$

Para los modelos que tienen verosimiltud se realizan con el `test = "LRT`, estos son el modelo de **Poisson** y la **Binomial Negativa**. En el caso de la **QuasiPoisson** se utliza el `test = "F"`, porque no tiene verosimilitud.


Para esta otra hipótesis: (comparaciones multiples)

$$H_0:\lambda_i=\lambda_j$$
Para los modelos de **Poisson** y la **Binomial Negativa**, usamos una $z$ (normal). En cambio la **Quasipoisson** es con la $t$


```{r}
drop1(mod2,test="F") # Este modelo es Quasipoisson
```

Note que como rechazamos la hipótesis nula, las medias de cada tratamiento son diferentes.


Para está 


### Código para usar emmeans en una Quasipoisson

```{r}
library(emmeans)
# 1. Extraer los grados de libertad residuales de tu modelo
gl_residuales <- df.residual(mod2)
# 2. Pasárselos al emmeans
diff = emmeans(mod2, pairwise ~ trat1, adjust = "bonferroni", df = mod2$df.residual, type = "response")
diff$contrasts
```


### Forma manual de hacer las estimaciones de una Quasipoisson

```{r}
med
```

```{r}
contrasts(trat1)
```

```{r}
cmie =c(1,1,0,0)
csir =c(1,0,1,0)
clec =c(1,0,0,1)
cvin =c(1,-1,-1,-1)
c.ms =cmie-csir
c.ml =cmie-clec
c.mv =cmie-cvin
c.sl =csir-clec
c.sv =csir-cvin
c.lv =clec-cvin
contr =cbind(c.ms,c.ml,c.mv,c.sl,c.sv,c.lv)
```


```{r}
b=mod2$coef
L=exp(t(contr)%*%b)
row.names(L)=c("miel-sirope","miel-leche","miel-vinagre",
"sirope-leche","sirope-vinagre","leche-vinagre")
round(L,2)
```
Estas cantidades representan la razón entre cada par de medias, por ejemplo, la media de miel es 19% mayor que la media de sirope, la media de miel es 6.7 veces la media de vinagre, etc. Se nota que las que más se diferencian son miel y vinagre, así como sirope y vinagre. Además la media de leche es 3.09 veces mayor a la de vinagre.



**Pruebas de hipótesis para las diferencias**


```{r}
cov=vcov(mod2)
ee=sqrt(diag(t(contr)%*%cov%*%contr))
eta=t(contr)%*%b
qt=eta/ee
p=pt(qt,28,lower.tail = F)
row.names(p)=row.names(L)
round(p,3)
```


### Nota importante sobre el emmeans 

Como el emmeans lo hace automáticamente de dos colas, entonces en este caso como son 6 diferencias lo compara contra $\alpha/12$, pero como a nosotros nos interesa de una cola entonces se deben comparar contra$\frac{p}{1} < \alpha$ o $p < 2\alpha$


# Binomial Negativa


Si los datos no cumplen el supuesto de una distribución Poisson, donde $E[Y|X]=V[Y|X]$. Se va a utilizar la distribución binomial negativa.  

Se tiene el siguiente modelo:
 
 $$\log(\lambda_j) = \beta_0+\tau_j$$
 
Esto nos lleva a que:
 
 
$$\sigma^2 = \lambda_j+\frac{\lambda_j^2}{\theta} =\lambda_j+\alpha \lambda^2$$

## Estimación de paramétros

```{r}
library(MASS)
mod3=glm.nb(conteo~trat1)
```


```{r}
drop1(mod3, test = "LRT")
```


```{r}
emmeans(mod3, pairwise~trat1, adjust = "bonferroni")$contrasts
```

# Prueba de hipótesis para saber cual modelo es mejor


$$H_0: Poiss \approx QP$$
$$H_1: QP  > Poiss$$

*En r* es lo mismo que `trafo1`

```{r, message=FALSE}
#install.packages("AER")
library(AER)
```

```{r}
dispersiontest(mod1,trafo=1)
```

$$H_0: Poiss \approx BN$$
$$H_1: BN > Poiss$$

*En r* es lo mismo que `trafo2`


## Resumen

\begin{enumerate}
    \item \textbf{Quasipoisson (QP)} $\rightarrow$ Evaluar el parámetro de dispersión ($\phi$):
    \begin{itemize}
        \item $\phi > 1 \rightarrow$ \textbf{Sobredispersión}: Se puede optar por Binomial Negativa (BN) o Quasipoisson (QP).
        \item $\phi < 1 \rightarrow$ \textbf{Subdispersión}: Se selecciona \textbf{Quasipoisson (QP)}.
    \end{itemize}
    
    \item Formular la prueba formal de dispersión usando la función: \texttt{dispersiontest()}
    
    \item \textbf{Decidir} el modelo definitivo entre: Quasipoisson (QP), Poisson (P) o Binomial Negativa (BN).
\end{enumerate}



En la **Quasipoisson**, la varianza se estima como:

$$\hat{\sigma}_j^2 = \phi\lambda_j$$

En la **Binomial Negativa**, la varianza se estima como:

$$\hat{\sigma}_j^2=\hat{\lambda_j}+\alpha \hat{\lambda^2}=\hat{\lambda_j}+\frac{\hat{\lambda_j^2}}{\theta}$$

*Nota* $\theta = \frac{1}{\alpha}$, este nos lo da r en el `summary()`, para el modelo de la binomial negativa.

*En r los hacemos de la siguiente manera*

```{r}
vobs=tapply(conteo,trat1,var) #varianza observada
med=tapply(predict(mod2,type="response"),trat1,mean) #media observada
#unique(predict(mod2,type="response")) Alternativa
phi=summary(mod2)$disp # Este es el theta o 1/alpha
vquasi=phi*med
alpha=1/summary(mod3)$theta
vnegbin=med+alpha*med^2
round(cbind(vobs,vquasi,vnegbin),2)
```
*Nota: Cuando hay covariables este análisis no se puede hacer, porque necesitamos al menos un dato por cada combinación con la covariable*.

La variancia de miel es subestimada por el modelo quasi-Poisson y sobrestimada por la binomial negativa, la de sirope está muy cerca la binomial negativa y lejos el quasi-Poisson, la de leche está parecida con las dos y la de vinagre está más cerca con la binomial negativa. No hay ninguna de las dos que realmente aproxime todas las variancias pero es un poco mejor con la binomial negativa.

# Análisis con bloques

Para esta clase de análisis pasamos de los modelos lineales generalizados corrientes a los modelos lineales generalizados mixtos debido que el número de niveles del estudio cambia.

## Poisson

El modelo es el siguiente:

$$\log(\lambda_{ij})=\beta_0+\tau_i+\delta_j$$

```{r}
mod4=glm(conteo~trat1+as.factor(unid),family=poisson)
sum(residuals(mod4,type="pearson")^2)/(32-11)
```

```{r}
dispersiontest(mod4,trafo=1,alternative = "less") 
#Este es el de subdispersión por el lees, es uan especie de guia para la hipótesis
```


Se rechaza la hipótesis de equidispersión y se concluye que la media es proporcional a la variancia con una constante de proporcionalidad menor a uno, es decir la variancia es menor que la media.

```{r}
mod5=glm(conteo~trat1+as.factor(unid),family=quasipoisson)
drop1(mod5,test="F")
```

Ahora el tratamiento resulta mucho más significativo con una probabilidad de error tipo I bajísima (<0.0001).

### Forma automática de los contrastes

```{r}
p = emmeans(mod5, pairwise~trat1, df = df.residual(mod5), adjust = "bonferroni")$contrast
p = as.data.frame(p)
round(p[,6]/12,5)
p[,6]/12<0.05/6
```

### Forma manual de los contrastes

```{r}
b=mod5$coef[1:4]
cov=vcov(mod5)[1:4,1:4]
ee=sqrt(diag(t(contr)%*%cov%*%contr))
eta=t(contr)%*%b
qt=eta/ee
p=pt(qt,21,lower.tail = F)
row.names(p)=row.names(L)
round(p,3)
```


```{r}
p<0.05/6
```
```{r}
t=qt(1-0.05/5,21) # note que es de una cola
LIM=exp(eta[-1]-t*ee[-1])
round(LIM,2)
```
```{r}
mod6=glm.nb(conteo~trat1+factor(unid))
summary(mod6)$theta
```

El parámetro de dispersión es 35839, el cual es suficientemente alto como para pensar que ** $\alpha_1/ \theta \to 0$. **Por lo tanto, la distribución binomial negativa tiende a la Poisson.

# Resumen de la diferencia entre un modelo de conteo y otro binomial

$$\log\left(\frac{\pi}{1-\pi}\right)=PL\to .OR$$


$$\log(\lambda)=PL\to \text{Razón de medias}$$